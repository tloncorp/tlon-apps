## Codebase Patterns
- Inline types are defined in `packages/shared/src/urbit/content.ts` with type guards like `isBold`, `isItalics`, etc.
- Story/Verse types are defined in `packages/shared/src/urbit/channel.ts`
- Use Vitest for tests in `packages/shared` - import from `vitest`
- Test files use `.test.ts` suffix in same directory as source files
- Block types include: Image, Header, Rule, Code, ListingBlock (contains Listing = List | ListItem)
- List structure: `{ list: { type: 'ordered'|'unordered'|'tasklist', contents: Inline[], items: Listing[] } }`
- ListItem structure: `{ item: Inline[] }`
- Task inline: `{ task: { checked: boolean, content: Inline[] } }` - used inside tasklist items
- Verse is `VerseInline | VerseBlock`, use `isBlockVerse()` from channel.ts to distinguish
- Blockquote is an Inline type with `{ blockquote: Inline[] }` structure
- markdown-it tokens: inline tokens use open/close pairs (e.g., `strong_open`/`strong_close`), content is in `.content`, attributes in `.attrs` array
- markdown-it block tokens: also use open/close pairs; use depth tracking to find matching close token
- markdown-it-task-lists plugin: adds `class="contains-task-list"` to list, checkbox rendered as html_inline token
- Type declarations for untyped npm packages go in `packages/shared/src/types/` as `.d.ts` files
- For re-exported modules, type declarations must exist in each consuming package; add to tsconfig `include` arrays
- `tiptap.diaryMixedToJSON(story)` converts Story → TipTap JSONContent; `editor.setContent()` applies it
- Tiptap extensions in @10play/tentap-editor bridges include built-in input rules (Markdown shortcuts) enabled by default

---

## 2026-01-21 - US-001
Thread: https://ampcode.com/threads/T-019be144-6510-7478-b334-d064c0dd0eea
- Implemented `inlinesToMarkdown(inlines: Inline[]): string` function
- Created `packages/shared/src/logic/storyToMarkdown.ts`
- Created `packages/shared/src/logic/storyToMarkdown.test.ts` with 13 tests
- Handles: string, Bold, Italics, Strikethrough, InlineCode, Link, Ship, Break
- Supports nested inlines recursively (e.g., bold within italic)
- **Learnings for future iterations:**
  - Inline type structure: strings are plain text, objects use specific keys (`bold`, `italics`, `strike`, `inline-code`, `link`, `ship`, `break`)
  - Link has `{ link: { href: string, content: string } }` structure
  - Ship has `{ ship: string }` where the string is the ship name without the `~` prefix
  - Use existing type guards from content.ts rather than creating new ones
---

## 2026-01-21 - US-002
Thread: https://ampcode.com/threads/T-019be146-9d5a-719c-8876-eb7257600a9e
- Implemented `blockToMarkdown(block: Block): string` function
- Added helper `listingToMarkdownLines()` for recursive list handling
- Handles: Header (h1-h6), Code (fenced blocks), Image, Rule, ListingBlock (ordered, unordered, tasklist)
- Supports nested lists with proper 2-space indentation
- Also added Task inline handling to `inlineToMarkdown()` for tasklist checkboxes
- Added 22 new tests (total now 35 tests in storyToMarkdown.test.ts)
- **Learnings for future iterations:**
  - List structure is complex: ListingBlock contains a Listing (which is List | ListItem)
  - List has `contents` (parent item text) and `items` (child items), both may be present
  - Nested lists pass down list type and adjust indentation
  - Task inlines have `{ task: { checked, content } }` structure - checkbox rendered in inline conversion
  - Rule block uses `{ rule: null }` similar to Break inline
---

## 2026-01-21 - US-003
Thread: https://ampcode.com/threads/T-019be14d-84f9-7244-a38d-e283f417ddf9
- Implemented `storyToMarkdown(story: Story): string` function
- Added `verseToMarkdown()` helper to dispatch VerseInline vs VerseBlock
- Added Blockquote handling to `inlineToMarkdown()` - prefixes each line with `> `
- Verses are joined with double newlines for paragraph separation
- Empty verses are skipped
- Handles null/undefined story gracefully
- Added 15 new tests (total now 50 tests in storyToMarkdown.test.ts)
- **Learnings for future iterations:**
  - Story is `Verse[]` where Verse is `VerseInline | VerseBlock`
  - VerseInline has `{ inline: Inline[] }`, VerseBlock has `{ block: Block }`
  - Use `isBlockVerse()` from `channel.ts` to distinguish between verse types
  - Blockquote is an Inline type with `{ blockquote: Inline[] }` structure
---

## 2026-01-21 - US-004
Thread: https://ampcode.com/threads/T-019be154-8ebc-7588-bc70-f36ec9cd33cc
- Added `markdown-it` and `markdown-it-task-lists` to packages/shared dependencies
- Added `@types/markdown-it` to devDependencies
- Verified packages install successfully with pnpm
- Typecheck passes
- **Learnings for future iterations:**
  - Dependencies are added via `pnpm add` in the package directory
  - The lockfile (pnpm-lock.yaml) is at the repo root and gets updated automatically
  - Use `pnpm -r tsc` to run typecheck across all packages in the monorepo
---

## 2026-01-21 - US-005
Thread: https://ampcode.com/threads/T-019be156-4b11-70f3-9ef7-bfe475c300d2
- Created `packages/shared/src/logic/markdownToStory.ts`
- Implemented `tokensToInlines(tokens: Token[]): Inline[]` function
- Handles: text → string, strong → Bold, em → Italics, s → Strikethrough, code_inline → InlineCode, link → Link, softbreak/hardbreak → Break
- Detects `~ship-name` pattern in text and converts to Ship inline
- Supports nested formatting (e.g., bold within italic)
- Uses open/close token pairing with `findCloseToken()` helper
- Created 23 unit tests covering all inline token types
- **Learnings for future iterations:**
  - markdown-it uses open/close token pairs (e.g., `strong_open`/`strong_close`) with `nesting` property
  - Inline tokens are in the `children` property of the `inline` block token
  - Link href is in `token.attrs` as `[['href', 'url']]` array format
  - Ship pattern: `~[a-z]{3,6}(-[a-z]{6})*` covers galaxies, stars, planets, and moons
  - Use `parseInline` for inline-only parsing, but full `parse` gives block context
---

## 2026-01-21 - US-006
Thread: https://ampcode.com/threads/T-019be15d-ee96-717a-a6da-68305d0a4966
- Implemented `tokenToBlock(tokens, startIndex)` function in `markdownToStory.ts`
- Returns `{ block, verse?, endIndex }` to support blockquotes returning VerseInline
- Handles: heading_open → Header, fence → Code, hr → Rule, paragraph_open with image → Image
- Handles: bullet_list_open → ListingBlock (unordered), ordered_list_open → ListingBlock (ordered)
- Handles: bullet_list with contains-task-list class → ListingBlock (tasklist) with Task inlines
- Handles: blockquote_open → VerseInline with Blockquote wrapper
- Supports nested lists with recursive parseListTokens()
- Created type declaration file for `markdown-it-task-lists` at `packages/shared/src/types/markdown-it-task-lists.d.ts`
- Added 23 new tests (total now 46 tests in markdownToStory.test.ts)
- Files changed: markdownToStory.ts, markdownToStory.test.ts, markdown-it-task-lists.d.ts
- **Learnings for future iterations:**
  - Block tokens use open/close pairs similar to inlines (e.g., `heading_open`/`heading_close`)
  - Task lists are detected via `class="contains-task-list"` attribute on bullet_list_open token
  - Task list items have an html_inline checkbox child; checked state is in `checked=""` attribute
  - Image tokens are inline children of paragraph tokens, not top-level block tokens
  - Blockquote converts to VerseInline with Blockquote inline (not a block type)
---

## 2026-01-21 - US-007
Thread: https://ampcode.com/threads/T-019be166-448f-737a-9a37-4e07c75e188d
- Implemented `markdownToStory(markdown: string): Story` function
- Added markdown-it parser instance with task-lists plugin to module
- Parses markdown to tokens and dispatches to VerseInline or VerseBlock
- Paragraph tokens → VerseInline with inline content
- Block tokens (heading, fence, hr, lists) → VerseBlock
- Blockquote tokens → VerseInline with Blockquote wrapper (via existing tokenToBlock)
- Handles empty/null/undefined input gracefully
- Added 21 new tests (total now 67 tests in markdownToStory.test.ts)
- Files changed: markdownToStory.ts, markdownToStory.test.ts
- **Learnings for future iterations:**
  - Paragraph content is in inline tokens which are children of `paragraph_open`/`paragraph_close` pairs
  - Use `tokenToBlock` to handle paragraph_open - it returns block for images, null for regular paragraphs
  - The markdown-it parser must be configured with task-lists plugin at module level for consistent usage
  - Story is `Verse[]` where each Verse is either `{ inline: Inline[] }` or `{ block: Block }`
---

## 2026-01-21 - US-009
Thread: https://ampcode.com/threads/T-019be173-eb68-7449-9ff9-7c8f7de1e805
- Added Markdown mode toggle button to BigInput toolbar
- Created new Markdown.svg icon (styled M with down arrow in rounded box)
- Added `isMarkdownMode` state to BigInput component
- Created markdownToggle toolbar item that:
  - Shows active state when in Markdown mode
  - Toggles `isMarkdownMode` state on press
  - Uses new 'Markdown' icon
- Toolbar item added at beginning of toolbar items list
- Files changed: BigInput.tsx, Markdown.svg (new), icons/index.ts
- **Learnings for future iterations:**
  - Toolbar items use ToolbarItem interface with onPress, active, disabled, icon properties
  - Custom toolbar items can be created without editor state by returning constant callbacks
  - Icons are 32x32 SVG using `currentColor` for stroke/fill
  - Icons are added to `packages/ui/src/assets/icons/` and exported from `index.ts`
---

## 2026-01-21 - US-008
Thread: https://ampcode.com/threads/T-019be176-d446-728e-ba2b-4fba86e9619e
- Created `packages/shared/src/logic/roundTrip.test.ts` with 55 round-trip conversion tests
- Tests Markdown → Story → Markdown preservation for: paragraphs, inline formatting, links, ship mentions, headers, code blocks, images, lists, task lists, horizontal rules, blockquotes, mixed content
- Tests Story → Markdown → Story preservation for all corresponding Story types
- Documented known lossy conversions in file comments:
  - Cite blocks: Story Cite type has no Markdown equivalent
  - BlockReference: No Markdown equivalent
  - Link content: Nested formatting in links is flattened to plain text
  - Image dimensions: width/height are lost when converting to Markdown
  - BlockCode: Different from fenced code blocks
  - Sect (sections): No direct Markdown equivalent
  - Tag: No direct Markdown equivalent
- Files changed: roundTrip.test.ts (new)
- **Learnings for future iterations:**
  - Round-trip tests are valuable for validating bidirectional conversion fidelity
  - Some loss is acceptable and documented (Cite, BlockReference, etc.)
  - Image dimensions (width/height) default to 0 when parsed from Markdown
  - Link content is always plain text in Story format, nested formatting is lost
---

## 2026-01-21 - US-010
Thread: https://ampcode.com/threads/T-019be17d-9178-747c-93dc-a49441269152
- Created `packages/app/ui/components/MarkdownEditor.tsx` component
- Uses monospace font (`fontFamily: '$mono'`) for code-like editing experience
- Full-width TextArea that expands with content
- Controlled input with `value` and `onChange` props
- Matches BigInput padding with `paddingHorizontal="$l"` and `paddingVertical="$m"`
- Supports placeholder text and testID for testing
- Files changed: MarkdownEditor.tsx (new)
- **Learnings for future iterations:**
  - Tamagui has `$mono` font token defined in `packages/ui/tamagui.config.ts`
  - TextArea from Tamagui is the standard multiline input component
  - Use `borderWidth={0}` and `borderColor="transparent"` for frameless appearance
  - Use `$tertiaryText` for placeholder color to match other inputs
---

## 2026-01-21 - US-011
Thread: https://ampcode.com/threads/T-019be17f-370c-717e-943d-1496069a5b91
- Integrated MarkdownEditor into BigInput component
- Added `markdownContent` state to track raw Markdown text
- Conditionally render MarkdownEditor or MessageInput based on `isMarkdownMode`
- MarkdownEditor receives `markdownContent` as value and `setMarkdownContent` as onChange
- Added `flex={1}` to the View wrapper so the MarkdownEditor fills available space
- Files changed: BigInput.tsx
- **Learnings for future iterations:**
  - The MarkdownEditor component already exists and takes `value` and `onChange` props
  - Conditional rendering in JSX works well for switching between editor modes
  - Added import for MarkdownEditor from `./MarkdownEditor`
---

## 2026-01-21 - US-012
Thread: https://ampcode.com/threads/T-019be181-459e-750d-86d1-ca85ee0b66c3
- Implemented rich text → Markdown conversion when toggling to Markdown mode
- Created `handleMarkdownToggle` callback in BigInput component
- Uses existing pattern: `tiptap.JSONToInlines(json)` → `constructStory(inlines)` → `storyToMarkdown(story)`
- Added `storyToMarkdown` export to `@tloncorp/shared` package
- Error handling with toast notification on conversion failure
- Files changed: BigInput.tsx, packages/shared/src/logic/index.ts
- **Learnings for future iterations:**
  - The `tiptap.JSONToInlines` converts Tiptap editor JSON to Inline array
  - `constructStory(inlines)` wraps inlines into proper Verse/Story structure
  - Toolbar items use double-callback pattern: `onPress: () => () => action()`
  - storyToMarkdown was not exported from shared package index - needed to add to logic/index.ts
---

## 2026-01-21 - US-013
Thread: https://ampcode.com/threads/T-019be184-8d97-73d4-a167-91f6a9f648ed
- Implemented Markdown → rich text conversion when toggling from Markdown mode
- Exported `markdownToStory` from `@tloncorp/shared` package
- Updated `handleMarkdownToggle` in BigInput.tsx to use `markdownToStory` and `tiptap.diaryMixedToJSON`
- Added error handling with toast notifications for conversion failures
- Added `markdown-it-task-lists.d.ts` type declarations to multiple packages (ui, app, tlon-web, tlon-mobile)
- Updated tsconfig.json files to include the new type declaration files
- Files changed: BigInput.tsx, packages/shared/src/logic/index.ts, multiple tsconfig.json files, 4 new .d.ts files
- **Learnings for future iterations:**
  - `tiptap.diaryMixedToJSON(story)` converts Story to TipTap JSONContent format
  - `editor.setContent(jsonContent)` updates the TipTap editor with new content
  - Type declarations for npm packages without @types need to be added to each package that uses them
  - Must update tsconfig.json `include` arrays to reference new .d.ts files in non-src locations
---

## 2026-01-21 - US-014
Thread: https://ampcode.com/threads/T-019be189-7131-71b8-b0be-9d4fdd268040
- Modified `handleSend` in BigInput.tsx to check `isMarkdownMode`
- When in Markdown mode, converts `markdownContent` to Story using `markdownToStory`
- Flattens Story (Verse[]) into (Inline | Block)[] for the draft content
- Error handling with toast notification if Markdown conversion fails
- Clears `markdownContent` and resets `isMarkdownMode` after successful send
- Added `Inline` and `Block` type imports from `@tloncorp/shared/urbit`
- Updated `handleSend` dependency array to include `isMarkdownMode`, `markdownContent`, and `showToast`
- Files changed: BigInput.tsx
- **Learnings for future iterations:**
  - `PostDataDraft.content` expects `(Inline | Block)[]` array, not Story
  - Story is `Verse[]` where `VerseInline` has `{ inline: Inline[] }` and `VerseBlock` has `{ block: Block }`
  - Use `flatMap` with explicit return type annotation to flatten Verse[] into (Inline | Block)[]
  - Type inference issues with flatMap require explicit type annotations: `(verse): (Inline | Block)[] => ...`
---

## 2026-01-21 - US-015
Thread: https://ampcode.com/threads/T-019be18d-a1f1-7028-9f21-bdd5f49549fb
- Enhanced `handleMarkdownToggle` in BigInput.tsx to handle existing post content
- When switching to Markdown mode:
  1. First tries to get content from Tiptap editor
  2. If editor is empty and editing an existing post, falls back to `editingPost.content.story`
  3. Converts story to Markdown using `storyToMarkdown`
- Uses `contentIsEmpty()` helper to check if editor has content
- Handles posts with no content gracefully (sets empty string)
- Added `editingPost` to handleMarkdownToggle dependency array
- Files changed: BigInput.tsx
- **Learnings for future iterations:**
  - `editingPost.content` has structure `{ story?: any }` where story is the Story array
  - The existing `contentIsEmpty()` function can be reused to check editor state
  - When editing a post, the Tiptap editor may not have loaded content yet when toggling modes
  - Fall back to the post's stored content if the editor appears empty
---

## 2026-01-21 - US-016
Thread: https://ampcode.com/threads/T-019be194-dba6-75db-aa93-b1e12ec108bf
- Verified Tiptap extensions already include Markdown input rules
- Created documentation at `packages/editor/src/markdownInputRules.md`
- Created test file at `packages/editor/src/markdownInputRules.test.ts` with 8 pattern tests
- Input rules already enabled via @tiptap/extension-* packages used by @10play/tentap-editor bridges
- No code changes needed to extensions themselves - just verification and documentation
- Files changed: markdownInputRules.md (new), markdownInputRules.test.ts (new)
- **Learnings for future iterations:**
  - Tiptap extensions (Bold, Italic, Code, Strike, Heading, BulletList, OrderedList, Blockquote, CodeBlock) have `addInputRules()` methods built-in
  - Input rules are enabled by default in tiptap (enableInputRules: true)
  - Mark input rules trigger on closing delimiter (e.g., `**text**`), not on space
  - Block input rules trigger on space after prefix (e.g., `# ` for heading)
  - The @10play/tentap-editor bridges pass through tiptap extensions with their input rules intact
  - Vitest is available in packages/editor for testing
---

## 2026-01-21 - US-017 (Bug Fixes)
Thread: BigInput Markdown mode fixes
- Fixed Post button not enabling when typing in Markdown mode
  - Added useEffect that updates `isEmpty` state based on `markdownContent` when in markdown mode
  - Also updates `hasContentChanges` for editing mode
- Fixed toolbar not showing when in Markdown mode
  - Toolbar requires a valid TlonEditorBridge, but editor is unmounted in Markdown mode
  - Created inline "Switch to Rich Text" toolbar bar for Markdown mode (both wide and narrow windows)
  - Wide: XStack with Markdown icon and "Switch to Rich Text" label below editor
  - Narrow: TouchableOpacity in floating format menu
- Updated MarkdownEditor styling
  - Added `minHeight={WINDOW_HEIGHT * 0.75}` for taller textarea (75% of viewport)
  - Added `outlineWidth={0}`, `outlineColor="transparent"`, and `focusStyle` to remove focus border
- Files changed: BigInput.tsx, MarkdownEditor.tsx
- **Learnings for future iterations:**
  - State updates in conditional editors need explicit handling - the main `handleEditorContentChanged` callback only fires from TipTap
  - Tamagui TextArea doesn't accept string viewport units like "75vh" - use numeric pixels instead
  - InputToolbar strictly requires a TlonEditorBridge - create alternative UI when editor unavailable
  - Text component size prop uses semantic tokens like `$label/s`, not simple tokens like `$s`
---

## 2026-01-21 - US-018 (Bug Fixes - Code Block Handling)
Thread: BigInput Markdown code block fixes
- Fixed markdown parser to trim whitespace from language strings
  - `token.info` from markdown-it may contain leading/trailing whitespace (e.g., ` js` instead of `js`)
  - Now trims language string before creating Code block
- Fixed HighlightedCode component to handle empty/invalid language strings
  - Previously failed with "Unknown language" error when `lang` was empty string
  - Now falls back to 'plaintext' for empty/falsy language strings
  - Added try/catch to gracefully handle unregistered languages
- Files changed: markdownToStory.ts, HighlightedCode.tsx
- **Learnings for future iterations:**
  - markdown-it `token.info` contains raw text after opening fence, may have whitespace
  - refractor.highlight() throws for unregistered languages - need try/catch for graceful degradation
  - Empty string `""` is falsy but !== undefined, so `lang ?? 'plaintext'` doesn't catch it (use `lang || 'plaintext'`)
---

## 2026-01-21 - US-019 (Code Block Backend Fix)
Thread: Strip code block language for Urbit backend
- Urbit backend doesn't support the `lang` field in code blocks - causes poke cast failures
- Modified markdownToStory.ts to omit `lang` field entirely from Code blocks
- Used TypeScript type assertion `as Code` since the interface requires `lang`
- Updated HighlightedCode.tsx to gracefully handle missing/empty `lang` with try/catch fallback
- Updated all related tests in markdownToStory.test.ts and roundTrip.test.ts
- Round-trip conversion is now lossy for code block language (documented in tests)
- Files changed: markdownToStory.ts, HighlightedCode.tsx, markdownToStory.test.ts, roundTrip.test.ts
- **Learnings for future iterations:**
  - Urbit backend (story-json Hoon lib) rejects Code blocks with `lang` field
  - TypeScript interfaces don't always match what backend accepts
  - Use type assertions cautiously when interface doesn't match actual requirements
  - Document lossy conversions in tests with clear comments
---
