## Codebase Patterns
- Inline types are defined in `packages/shared/src/urbit/content.ts` with type guards like `isBold`, `isItalics`, etc.
- Story/Verse types are defined in `packages/shared/src/urbit/channel.ts`
- Use Vitest for tests in `packages/shared` - import from `vitest`
- Test files use `.test.ts` suffix in same directory as source files
- Block types include: Image, Header, Rule, Code, ListingBlock (contains Listing = List | ListItem)
- List structure: `{ list: { type: 'ordered'|'unordered'|'tasklist', contents: Inline[], items: Listing[] } }`
- ListItem structure: `{ item: Inline[] }`
- Task inline: `{ task: { checked: boolean, content: Inline[] } }` - used inside tasklist items
- Verse is `VerseInline | VerseBlock`, use `isBlockVerse()` from channel.ts to distinguish
- Blockquote is an Inline type with `{ blockquote: Inline[] }` structure
- markdown-it tokens: inline tokens use open/close pairs (e.g., `strong_open`/`strong_close`), content is in `.content`, attributes in `.attrs` array
- markdown-it block tokens: also use open/close pairs; use depth tracking to find matching close token
- markdown-it-task-lists plugin: adds `class="contains-task-list"` to list, checkbox rendered as html_inline token
- Type declarations for untyped npm packages go in `packages/shared/src/types/` as `.d.ts` files

---

## 2026-01-21 - US-001
Thread: https://ampcode.com/threads/T-019be144-6510-7478-b334-d064c0dd0eea
- Implemented `inlinesToMarkdown(inlines: Inline[]): string` function
- Created `packages/shared/src/logic/storyToMarkdown.ts`
- Created `packages/shared/src/logic/storyToMarkdown.test.ts` with 13 tests
- Handles: string, Bold, Italics, Strikethrough, InlineCode, Link, Ship, Break
- Supports nested inlines recursively (e.g., bold within italic)
- **Learnings for future iterations:**
  - Inline type structure: strings are plain text, objects use specific keys (`bold`, `italics`, `strike`, `inline-code`, `link`, `ship`, `break`)
  - Link has `{ link: { href: string, content: string } }` structure
  - Ship has `{ ship: string }` where the string is the ship name without the `~` prefix
  - Use existing type guards from content.ts rather than creating new ones
---

## 2026-01-21 - US-002
Thread: https://ampcode.com/threads/T-019be146-9d5a-719c-8876-eb7257600a9e
- Implemented `blockToMarkdown(block: Block): string` function
- Added helper `listingToMarkdownLines()` for recursive list handling
- Handles: Header (h1-h6), Code (fenced blocks), Image, Rule, ListingBlock (ordered, unordered, tasklist)
- Supports nested lists with proper 2-space indentation
- Also added Task inline handling to `inlineToMarkdown()` for tasklist checkboxes
- Added 22 new tests (total now 35 tests in storyToMarkdown.test.ts)
- **Learnings for future iterations:**
  - List structure is complex: ListingBlock contains a Listing (which is List | ListItem)
  - List has `contents` (parent item text) and `items` (child items), both may be present
  - Nested lists pass down list type and adjust indentation
  - Task inlines have `{ task: { checked, content } }` structure - checkbox rendered in inline conversion
  - Rule block uses `{ rule: null }` similar to Break inline
---

## 2026-01-21 - US-003
Thread: https://ampcode.com/threads/T-019be14d-84f9-7244-a38d-e283f417ddf9
- Implemented `storyToMarkdown(story: Story): string` function
- Added `verseToMarkdown()` helper to dispatch VerseInline vs VerseBlock
- Added Blockquote handling to `inlineToMarkdown()` - prefixes each line with `> `
- Verses are joined with double newlines for paragraph separation
- Empty verses are skipped
- Handles null/undefined story gracefully
- Added 15 new tests (total now 50 tests in storyToMarkdown.test.ts)
- **Learnings for future iterations:**
  - Story is `Verse[]` where Verse is `VerseInline | VerseBlock`
  - VerseInline has `{ inline: Inline[] }`, VerseBlock has `{ block: Block }`
  - Use `isBlockVerse()` from `channel.ts` to distinguish between verse types
  - Blockquote is an Inline type with `{ blockquote: Inline[] }` structure
---

## 2026-01-21 - US-004
Thread: https://ampcode.com/threads/T-019be154-8ebc-7588-bc70-f36ec9cd33cc
- Added `markdown-it` and `markdown-it-task-lists` to packages/shared dependencies
- Added `@types/markdown-it` to devDependencies
- Verified packages install successfully with pnpm
- Typecheck passes
- **Learnings for future iterations:**
  - Dependencies are added via `pnpm add` in the package directory
  - The lockfile (pnpm-lock.yaml) is at the repo root and gets updated automatically
  - Use `pnpm -r tsc` to run typecheck across all packages in the monorepo
---

## 2026-01-21 - US-005
Thread: https://ampcode.com/threads/T-019be156-4b11-70f3-9ef7-bfe475c300d2
- Created `packages/shared/src/logic/markdownToStory.ts`
- Implemented `tokensToInlines(tokens: Token[]): Inline[]` function
- Handles: text → string, strong → Bold, em → Italics, s → Strikethrough, code_inline → InlineCode, link → Link, softbreak/hardbreak → Break
- Detects `~ship-name` pattern in text and converts to Ship inline
- Supports nested formatting (e.g., bold within italic)
- Uses open/close token pairing with `findCloseToken()` helper
- Created 23 unit tests covering all inline token types
- **Learnings for future iterations:**
  - markdown-it uses open/close token pairs (e.g., `strong_open`/`strong_close`) with `nesting` property
  - Inline tokens are in the `children` property of the `inline` block token
  - Link href is in `token.attrs` as `[['href', 'url']]` array format
  - Ship pattern: `~[a-z]{3,6}(-[a-z]{6})*` covers galaxies, stars, planets, and moons
  - Use `parseInline` for inline-only parsing, but full `parse` gives block context
---

## 2026-01-21 - US-006
Thread: https://ampcode.com/threads/T-019be15d-ee96-717a-a6da-68305d0a4966
- Implemented `tokenToBlock(tokens, startIndex)` function in `markdownToStory.ts`
- Returns `{ block, verse?, endIndex }` to support blockquotes returning VerseInline
- Handles: heading_open → Header, fence → Code, hr → Rule, paragraph_open with image → Image
- Handles: bullet_list_open → ListingBlock (unordered), ordered_list_open → ListingBlock (ordered)
- Handles: bullet_list with contains-task-list class → ListingBlock (tasklist) with Task inlines
- Handles: blockquote_open → VerseInline with Blockquote wrapper
- Supports nested lists with recursive parseListTokens()
- Created type declaration file for `markdown-it-task-lists` at `packages/shared/src/types/markdown-it-task-lists.d.ts`
- Added 23 new tests (total now 46 tests in markdownToStory.test.ts)
- Files changed: markdownToStory.ts, markdownToStory.test.ts, markdown-it-task-lists.d.ts
- **Learnings for future iterations:**
  - Block tokens use open/close pairs similar to inlines (e.g., `heading_open`/`heading_close`)
  - Task lists are detected via `class="contains-task-list"` attribute on bullet_list_open token
  - Task list items have an html_inline checkbox child; checked state is in `checked=""` attribute
  - Image tokens are inline children of paragraph tokens, not top-level block tokens
  - Blockquote converts to VerseInline with Blockquote inline (not a block type)
---

## 2026-01-21 - US-007
Thread: https://ampcode.com/threads/T-019be166-448f-737a-9a37-4e07c75e188d
- Implemented `markdownToStory(markdown: string): Story` function
- Added markdown-it parser instance with task-lists plugin to module
- Parses markdown to tokens and dispatches to VerseInline or VerseBlock
- Paragraph tokens → VerseInline with inline content
- Block tokens (heading, fence, hr, lists) → VerseBlock
- Blockquote tokens → VerseInline with Blockquote wrapper (via existing tokenToBlock)
- Handles empty/null/undefined input gracefully
- Added 21 new tests (total now 67 tests in markdownToStory.test.ts)
- Files changed: markdownToStory.ts, markdownToStory.test.ts
- **Learnings for future iterations:**
  - Paragraph content is in inline tokens which are children of `paragraph_open`/`paragraph_close` pairs
  - Use `tokenToBlock` to handle paragraph_open - it returns block for images, null for regular paragraphs
  - The markdown-it parser must be configured with task-lists plugin at module level for consistent usage
  - Story is `Verse[]` where each Verse is either `{ inline: Inline[] }` or `{ block: Block }`
---

## 2026-01-21 - US-009
Thread: https://ampcode.com/threads/T-019be173-eb68-7449-9ff9-7c8f7de1e805
- Added Markdown mode toggle button to BigInput toolbar
- Created new Markdown.svg icon (styled M with down arrow in rounded box)
- Added `isMarkdownMode` state to BigInput component
- Created markdownToggle toolbar item that:
  - Shows active state when in Markdown mode
  - Toggles `isMarkdownMode` state on press
  - Uses new 'Markdown' icon
- Toolbar item added at beginning of toolbar items list
- Files changed: BigInput.tsx, Markdown.svg (new), icons/index.ts
- **Learnings for future iterations:**
  - Toolbar items use ToolbarItem interface with onPress, active, disabled, icon properties
  - Custom toolbar items can be created without editor state by returning constant callbacks
  - Icons are 32x32 SVG using `currentColor` for stroke/fill
  - Icons are added to `packages/ui/src/assets/icons/` and exported from `index.ts`
---

## 2026-01-21 - US-008
Thread: https://ampcode.com/threads/T-019be176-d446-728e-ba2b-4fba86e9619e
- Created `packages/shared/src/logic/roundTrip.test.ts` with 55 round-trip conversion tests
- Tests Markdown → Story → Markdown preservation for: paragraphs, inline formatting, links, ship mentions, headers, code blocks, images, lists, task lists, horizontal rules, blockquotes, mixed content
- Tests Story → Markdown → Story preservation for all corresponding Story types
- Documented known lossy conversions in file comments:
  - Cite blocks: Story Cite type has no Markdown equivalent
  - BlockReference: No Markdown equivalent
  - Link content: Nested formatting in links is flattened to plain text
  - Image dimensions: width/height are lost when converting to Markdown
  - BlockCode: Different from fenced code blocks
  - Sect (sections): No direct Markdown equivalent
  - Tag: No direct Markdown equivalent
- Files changed: roundTrip.test.ts (new)
- **Learnings for future iterations:**
  - Round-trip tests are valuable for validating bidirectional conversion fidelity
  - Some loss is acceptable and documented (Cite, BlockReference, etc.)
  - Image dimensions (width/height) default to 0 when parsed from Markdown
  - Link content is always plain text in Story format, nested formatting is lost
---
