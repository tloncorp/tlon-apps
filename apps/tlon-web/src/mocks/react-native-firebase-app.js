// src/mocks/react-native-firebase-app.js
const mockFirebaseApp = {
  initializeApp: () => {},
  app: () => ({
    // Mock app methods
  }),
  // Add any other methods or properties used in your app
};

export default mockFirebaseApp;
