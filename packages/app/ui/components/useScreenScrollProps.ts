import type {
  ScreenScrollProps,
  UseScreenScrollPropsOptions,
} from './useScreenScrollProps.types';

export function useScreenScrollProps(
  _options: UseScreenScrollPropsOptions = {}
): ScreenScrollProps {
  return {};
}
