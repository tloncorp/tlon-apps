import { DeleteSheet } from '../../ui';

export default (
  <DeleteSheet
    open={true}
    onOpenChange={() => {}}
    deleteAction={() => {}}
    title="this thing"
    itemTypeDescription="special thing"
  />
);
