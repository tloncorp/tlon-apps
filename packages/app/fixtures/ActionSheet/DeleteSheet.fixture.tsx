import { DeleteSheet } from '@tloncorp/ui';

export default (
  <DeleteSheet
    open={true}
    onOpenChange={() => {}}
    deleteAction={() => {}}
    title="this thing"
    itemTypeDescription="special thing"
  />
);
