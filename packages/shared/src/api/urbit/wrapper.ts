import { createMockedFunction } from '../../utils';
import * as base from './base';

export const scry = createMockedFunction(base.scry);
export const subscribe = createMockedFunction(base.subscribe);
