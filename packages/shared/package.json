{
  "name": "@tloncorp/shared",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/db/* src/urbit/* --format esm --minify --dts --out-dir dist",
    "dev": "npm run build -- --watch",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "big-integer": "^1.6.52",
    "drizzle-orm": "^0.30.3",
    "sorted-btree": "^1.8.1",
    "expo-sqlite": "~13.3.0"
  }
}
