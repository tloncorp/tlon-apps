{
  "name": "@tloncorp/shared",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/urbit/* src/api/* src/sync --format esm --minify --dts --out-dir dist",
    "dev": "npm run build -- --watch",
    "postinstall": "npm run build",
    "test": "vitest --watch false"
  },
  "dependencies": {
    "big-integer": "^1.6.52",
    "sorted-btree": "^1.8.1",
    "@urbit/http-api": "^3.1.0-dev-2",
    "@urbit/aura": "^1.0.0"
  },
  "devDependencies": {
    "vitest": "^1.0.4"
  }
}
