{
  "name": "@tloncorp/shared",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/db/* src/urbit/* src/api/* src/sync --format esm --minify --dts --out-dir dist",
    "dev": "npm run build -- --watch",
    "postinstall": "npm run build",
    "test": "vitest --watch false"
  },
  "dependencies": {
    "@urbit/api": "^2.3.0",
    "@urbit/aura": "^1.0.0",
    "@urbit/http-api": "^3.1.0-dev-2",
    "big-integer": "^1.6.52",
    "drizzle-orm": "^0.30.3",
    "sorted-btree": "^1.8.1"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "vitest": "^1.0.4"
  }
}
