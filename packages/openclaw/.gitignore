node_modules/
dist/
src/version.generated.ts
*.log
.DS_Store
# every env variant, not just `.env` — per-run files like
# `.env.surfaces-6a` hold ship codes and provider keys, and a rule that
# names only `.env` leaves them committable
.env
.env.*
!.env.example
.vscode
WORKLOG.md
.pnpm-store/

# Publish staging
.publish/
*.tgz

# Workspace tlon CLI staged for the e2e container (tlon-bot-e2e
# openclaw driver beforeComposeBuild) — a ~99MB platform binary.
dev/tlon-skill-workspace-bin/
