services:
  fake-model:
    image: node:22-bookworm-slim
    working_dir: /workspace/openclaw-tlon
    command: ["node", "test/support/fake-model/server.mjs"]
    volumes:
      - ..:/workspace/openclaw-tlon
    expose:
      - "4000"
    ports:
      - "${FAKE_MODEL_PORT:-4000}:4000"

  # Both fakezod ships in one container (they need shared fake ames)
  ships:
    # Node's Debian image supplies a glibc runtime, tar/gzip, and an HTTPS
    # client without an apt-get at container startup. Avoiding Ubuntu's HTTP
    # package mirror keeps ship boot independent of ports.ubuntu.com.
    image: node:22-bookworm-slim
    command:
      - bash
      - -c
      - |
        set -e
        cd /data

        download() {
          node - "$$1" "$$2" "$$3" "$$4" "$$5" <<'NODE'
        const crypto = require('node:crypto');
        const fs = require('node:fs');
        const { Readable } = require('node:stream');
        const { pipeline } = require('node:stream/promises');
        const [url, output, digestKind, expectedDigest, expectedSize] =
          process.argv.slice(2);

        async function verify() {
          const stat = await fs.promises.stat(output);
          if (stat.size !== Number(expectedSize)) {
            throw new Error(
              `size mismatch: expected $${expectedSize}, got $${stat.size}`
            );
          }
          const hash = crypto.createHash(digestKind);
          for await (const chunk of fs.createReadStream(output)) {
            hash.update(chunk);
          }
          const actualDigest = hash.digest('hex');
          if (actualDigest !== expectedDigest) {
            throw new Error(
              `$${digestKind} mismatch: expected $${expectedDigest}, got $${actualDigest}`
            );
          }
          console.error(
            `verified $${output}: $${stat.size} bytes, $${digestKind}=$${actualDigest}`
          );
        }

        (async () => {
          let lastError;
          for (let attempt = 1; attempt <= 3; attempt += 1) {
            try {
              const response = await fetch(url, {
                redirect: 'follow',
                signal: AbortSignal.timeout(300_000),
              });
              if (!response.ok || !response.body) {
                throw new Error(`HTTP $${response.status}`);
              }
              await pipeline(
                Readable.fromWeb(response.body),
                fs.createWriteStream(output)
              );
              await verify();
              return;
            } catch (error) {
              lastError = error;
              fs.rmSync(output, { force: true });
              console.error(`download attempt $${attempt} failed: $${error}`);
              await new Promise((resolve) => setTimeout(resolve, 2_000));
            }
          }
          throw lastError;
        })().catch((error) => {
          console.error(error);
          process.exit(1);
        });
        NODE
        }

        # Pin both halves of the runtime/snapshot pair so the version skew that
        # crashed serf replay (stale snapshot -> downgrade runtime to replay ->
        # serf unexpectedly shut down) cannot silently return when `latest`
        # advances past the snapshot kelvin. rube-*-group-blob is the archive
        # generation the tlon-web e2e suite runs; vere-v4.5 is the matching
        # runtime. Keep this in lockstep with
        # packages/tlon-bot-e2e/docker/docker-compose.base.yml: both harnesses
        # run test/cases/, and case 08 scries %steward, which only exists on
        # piers built from a post-steward-refactor desk.
        echo "==> vere pin: vere-v4.5  pier gen: group-blob"

        echo "==> Downloading zod..."
        download https://bootstrap.urbit.org/rube-zod-group-blob.tgz rube-zod-group-blob.tgz md5 54eddbbd4ba4aea4dfddf32572696e4a 339451899 || { echo "Failed to download zod"; exit 1; }
        ls -la rube-zod-group-blob.tgz
        echo "==> Extracting zod..."
        tar xzf rube-zod-group-blob.tgz || { echo "Failed to extract zod"; exit 1; }

        echo "==> Downloading ten..."
        download https://bootstrap.urbit.org/rube-ten-group-blob.tgz rube-ten-group-blob.tgz md5 665a73f4eb2e910abe3b0f2c156f1ad6 338626853 || { echo "Failed to download ten"; exit 1; }
        ls -la rube-ten-group-blob.tgz
        echo "==> Extracting ten..."
        tar xzf rube-ten-group-blob.tgz || { echo "Failed to extract ten"; exit 1; }

        echo "==> Downloading mug..."
        download https://bootstrap.urbit.org/rube-mug-group-blob.tgz rube-mug-group-blob.tgz md5 90ec6c8c6640dab832358bf4fe30b8c7 339572369 || { echo "Failed to download mug"; exit 1; }
        ls -la rube-mug-group-blob.tgz
        echo "==> Extracting mug..."
        tar xzf rube-mug-group-blob.tgz || { echo "Failed to extract mug"; exit 1; }

        echo "==> Downloading vere..."
        download https://github.com/urbit/vere/releases/download/vere-v4.5/linux-x86_64.tgz vere.tgz sha256 bf7a30aad0a820bf217f3f81239b1029d16f11ba5274d45067d811489f817eed 9728849 || { echo "Failed to download vere"; exit 1; }
        tar xzf vere.tgz || { echo "Failed to extract vere"; exit 1; }
        VERE=$$(ls -1 vere-*-linux-x86_64 | head -1)
        chmod +x $$VERE
        echo "==> Using vere: $$VERE"

        echo "==> Starting ships..."
        ./$$VERE -t --loom 31 --http-port 8080 zod &
        ./$$VERE -t --loom 31 --http-port 8081 ten &
        ./$$VERE -t --loom 31 --http-port 8082 mug
    volumes:
      - ships-data:/data
    ports:
      - "${ZOD_PORT:-8080}:8080"
      - "${TEN_PORT:-8081}:8081"
      - "${MUG_PORT:-8082}:8082"

  # OpenClaw gateway with plugin
  openclaw:
    build:
      context: .
      dockerfile: Dockerfile.test
      args:
        OPENCLAW_CORE_VERSION: ${OPENCLAW_CORE_VERSION:-2026.8.2}
    environment:
      - OPENCLAW_CORE_VERSION=${OPENCLAW_CORE_VERSION:-2026.8.2}
      - TLON_URL=${TLON_URL:-http://ships:8080}
      - TLON_SHIP=${TLON_SHIP:-~zod}
      - TLON_CODE=${TLON_CODE:-lidlut-tabwed-pillex-ridrup}
      - TLON_OWNER_SHIP=${TLON_OWNER_SHIP:-~ten}
      - TLON_DM_ALLOWLIST=${TLON_DM_ALLOWLIST:-~ten}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - BRAVE_API_KEY=${BRAVE_API_KEY}
      - MODEL=${MODEL:-custom-proxy/tlon-test-scripted}
      - TLONBOT_TOKEN=${TLONBOT_TOKEN}
      - VERBOSE=${VERBOSE:-0}
      - TEST_LIVE_TOOL_TRACE=${TEST_LIVE_TOOL_TRACE:-0}
      - TEST_LIVE_TOOL_TRACE_CONTENTS=${TEST_LIVE_TOOL_TRACE_CONTENTS:-0}
    ports:
      - "${OPENCLAW_GATEWAY_PORT:-18789}:18789"
    depends_on:
      - fake-model
    volumes:
      - ..:/workspace/openclaw-tlon
      - openclaw-modules:/workspace/tlon/node_modules

volumes:
  ships-data:
  openclaw-modules:
