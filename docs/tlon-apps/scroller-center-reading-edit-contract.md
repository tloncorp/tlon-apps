# Center reading point during an actual edit

This supplements the original `edit-growth-shrink` attempt; it does not change
that raw recording or promote its result. The original case sampled the first
row while editing a later row. That cannot establish the accepted center-visible
reading contract when the changed row lies between the first and center rows.

Before the edit, establish deliberate READ with real wheel input. Acquire one
unchanged, uniquely identified message and an exact non-whitespace character
near the visible viewport center. Record its declared text, post ID, character
offset, viewport-relative coordinate and neighboring row geometry. Require that
the edited message is visible and above that reading point. Use the real Edit
menu, composer and Save control to expand and then shrink the message. Prove
each saved revision and actual row-height change before the next action.

The unchanged character must retain its viewport-relative vertical coordinate
within the existing 1 CSS px tolerance throughout the capture and the fixed
one-second terminal tail. A necessary legal-range clamp must be computed from
the measured legal range, never treated as restored FOLLOW. Preserve the raw
sampling cadence and report incomplete acquisition separately. The text must
remain the declared text; no substring substitution or whitespace normalization.

User-opened action menus may intentionally cover content. Any exposure claim
must state those intervals explicitly; the geometric reading assertion must
not silently become a full paint or unobstructed-content claim. Do not assert
that all other rows retain their old top: rows separated by the resized message
have different expected positions. Record those row changes as witnesses.

Further cases need distinct preconditions: mutation below the reading point,
mutation of the reading message itself, and removal requiring the accepted
forward/backward fallback. This one case cannot qualify those alternatives.

The first executable slice uses 36 declared plain-text production posts, normal
app flags and the ordinary headless runner. It requires an unclamped READ range
throughout; reaching either boundary makes this bounded slice incomplete. No
asynchronous range join is used to excuse movement. The existing reading
collector and exposure oracle are unchanged. Replay returns the original
exposure assessment separately and does not qualify unobstructed content while
the user opens the real message menu. Point drift, hidden text, changed identity
and acquisition gaps remain errors. An obstruction outside an observed menu
interval fails. Two exact UI edit requests, complete durable 36-post windows and
actual row growth/shrink must qualify before the result can pass.

For a focused run, use `playwright.scroller-product.config.ts` and the exact
`editing a visible message above the center reading character preserves that
character` title. `scripts/scroll-stability-center-edit-evidence.mjs` provides
the standalone raw replay; supply the enclosing Playwright result start time
and duration. Diagnostic producer assessments do not substitute for replay.
The original standalone reader is now also registered in the full 43-case
product reporter. Its exact title, unique proof, actual enclosing attempt clock
and immutable contract are required; duplicate, missing or invalid evidence
remains incomplete. This integration does not broaden its above-point scope.

Evidence finalization freezes and exports both collectors before waiting for
transport completion. A separately attached frozen capture survives incomplete
network evidence. Header collection has a one-second teardown bound; a missing
response remains incomplete. An actual 204 response with a measured header time
and exact subsequent durable content qualifies edit delivery even when Chromium
never reports response end. That endpoint stays null/unknown, never inferred
from headers or the durable read, and no response-end latency is claimed.
Controls must reject absent headers, malformed or reversed timing, and incorrect
durable content, while retaining capture export before a stalled transport wait.

The durable verification GET must start after the matching PUT response headers,
not merely finish later. The helper waits for that exact request index, post ID
and requested essay before issuing a fresh GET. An optimistic UI change or a
concurrent GET is insufficient, even when its content ultimately agrees. The
original overlapping-read attempt remains incomplete; this correction changes
request scheduling and preserves the independent ordering guard.

## Recorded attempts — 2026-09-07

All attempts remain preserved; later results do not replace their original
verdicts. These are normal-flag desktop Vite runs, not built-asset or compositor
qualification.

| Attempt | Result and retained evidence |
| --- | --- |
| Center R1 | Setup incomplete: scrolling changed a positional child selector; the exact canonical reader remained. `/private/tmp/scroller-center-edit-product-r1-20260907` |
| Center R2 | Teardown incomplete: both edits ran, but awaiting a never-finishing 204 body prevented raw sampler export before the deadline. `/private/tmp/scroller-center-edit-product-r2-20260907` |
| Combined center/keyboard R1 | Center incomplete: raw capture and the original reading/exposure oracle passed, but verification GETs began before matching response headers. `/private/tmp/scroller-center-keyboard-product-r1-20260907` |
| Center R3 | Actual and independent sampled PASS in 20.457 seconds, with fresh GETs after matching headers. `/private/tmp/scroller-center-edit-product-r3-20260907` |

R3 retained 171 samples, a 92.8 ms maximum reading gap, 3.4 ms maximum acquisition,
0.1953125 px maximum unchanged-character movement and a 1,013.6 ms terminal tail.
The edited row grew from 31.203125 to 157.5078125 px, then shrank to 52.5078125 px.
Both actual 204 responses preceded their verification GETs by 11.059 and 5.617 ms;
four complete 36-post reads independently qualified exact revisions and all
unchanged posts. Response ends remain unknown. The original reading/exposure
oracle passed with no issues; presented frames remain unqualified. Source and
independent-reader snapshots matched throughout capture and replay.

The same combined run's latest-keyboard test passed its actual assertions.
Its separate unchanged geometry oracle qualified 897 samples, maximum spacing
57.4 ms and maximum bottom gap 0.5 px. The full existing keyboard importer still
rejects headless provenance as incomplete; that gate was not relaxed.

Portable controls use the repository's Node 22 version:

```sh
node --experimental-strip-types --test scripts/scroll-stability-center-edit-evidence.controls.mjs scripts/scroll-stability-center-edit-finalization.controls.mjs scripts/scroll-stability-center-edit-locator.controls.mjs
```

The final 42 focused controls pass. The request-order controls first reproduced
four failures. Helper, spec and controls use repository-relative imports; local
artifact directories above are evidence references, not runtime dependencies.

## Below-point variant and historical first-row evidence

The separate below-point contract was declared before its controls in
`/private/tmp/scroller-below-center-edit-20260908/acceptance.md`. It replaces the
active first-row `edit-growth-shrink` assertion; the original raw attempts and
their original first-row oracle remain available through the historical importer.
The active product selection still has 43 cases.

Establish deliberate READ with wheel input, retain the exact unchanged character
in canonical row 18 near the visible center, and edit the initially exposed row
19 below it through the real Edit menu, composer and Save action. Prove the
expanded and shrunken revisions with fresh durable reads and actual height
changes. The center character must stay within 1 CSS px throughout both edits
and the fixed 1,000 ms tail. The existing 100 ms sampling, 32 ms measurement and
unclamped-range requirements remain unchanged. Menu obstruction remains a
separate dimension. Do not substitute a row-edge assertion or reacquire a
different character after the edit.

The exact registered title selects the below-point corpus and spatial contract;
raw evidence cannot choose its own side or disable replay by changing its
attached contract. The original above-point title, corpus and spatial checks
retain their behavior. Both variants share the actual interaction helper and
the existing independent reader. The below variant has no product pass yet at
this integration checkpoint.

The reviewed candidate passes 69 focused controls and scoped TypeScript. Those
include the retained above-point and finalization controls, wrong-side/identity
counterexamples, historical first-row replay, and rejection of a forged raw
contract that would otherwise bypass the below proof. A subsequent integrated
app check passes 237 registry/replay assertions. Evidence:
`/private/tmp/scroller-below-center-edit-20260908/handoff.json` and
`/private/tmp/scroller-below-center-edit-20260908/integrated-app.json`.
