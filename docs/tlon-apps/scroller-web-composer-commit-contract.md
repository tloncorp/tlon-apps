The r3 actual keyboard capture has a transient11px latest gap after Shift+Enter
grows the input. It later corrects; the older persistent gap does not recur.
Preserve both raw outcomes. The new work must reconcile the existing scroll
owner synchronously after the input's DOM height measurement commits, before
returning to input observers. ResizeObserver remains a fallback for external
layout changes. Do not turn a resize into new FOLLOW authority.

Use the current conversation's registration and coordinator. FOLLOW should
reach the measured legal end; READ keeps its existing interior reading point;
TARGET retains its current destination policy. Missing, retired, hidden or
covered owners perform no scroll work. The notify path must not consume or
replace a separate capture/restore request. Native input behavior is unchanged.

Controls must exercise these ownership/geometry behaviors through the real
coordinator and registry, including an upward gesture and a retired registration.
The actual latest-keyboard case must keep the existing1px tolerance, timing,
input/focus/selection/delivery criteria and raw sample acquisition checks. A
headless run cannot qualify any headed/presented-frame requirement. Do not
claim the brief sampled gap was visibly painted without presentation evidence.
