The following tools are available to aid developing on urbit:
`backend/run-tests.sh` - run the whole suite of backend tests: unit tests and aqua tests
`backend/update-pill.sh` - generate a new pill containing specified base and groups desks
`backend/gen-moon.sh` - generate a new moon
