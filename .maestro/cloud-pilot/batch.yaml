appId: ${MAESTRO_APP_ID}
name: Maestro ship Home group and profile controls
---
- runFlow: subflows/login.yaml
- runFlow: subflows/contacts-tab.yaml
- tapOn: '^You$'
- copyTextFrom:
    text: '^~[a-z]+(?:-[a-z]+)*$'
- evalScript: ${output.cloudShip = maestro.copiedText}
- assertTrue: ${output.cloudShip === '~batbet-litnec'}
- tapOn: '^Back$'
- runFlow: subflows/home-tab.yaml
- runFlow:
    file: home-groups.yaml
    env:
      MAESTRO_SESSION: warm
      MAESTRO_TEST_SHIP: ${output.cloudShip}
- runFlow:
    file: profile.yaml
    env:
      MAESTRO_SESSION: warm
      MAESTRO_TEST_SHIP: ${output.cloudShip}
