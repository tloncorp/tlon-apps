const original = 'default';
const target = '#008EFF';
output.profileColor = {
  original,
  originalLabel: `^Profile avatar color ${original}$`,
  target,
  targetId: 'ProfileColorSwatch-008EFF',
  targetLabel: `^Profile avatar color ${target}$`,
  changed: false,
};
