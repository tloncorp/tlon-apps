/-  g=groups
/+  j=groups-json
|_  ships=(set ship)
++  grad  %noun
++  grow
  |%
  ++  noun  ships
  ++  json  `^json`a/(turn ~(tap in ships) ship:enjs:j)
  --
++  grab
  |%
  ++  noun  ,(set ship)
  --
--
