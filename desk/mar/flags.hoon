/-  g=groups
/+  j=groups-json
|_  flags=(set flag:g)
++  grad  %noun
++  grow
  |%
  ++  noun  flags
  ++  json  `^json`a/(turn ~(tap in flags) |=(f=flag:g s/(flag:enjs:j f)))
  --
++  grab
  |%
  ++  noun  ,(set flag:g)
  --
--
