diff --git a/lib/module/ReanimatedModule/js-reanimated/index.js b/lib/module/ReanimatedModule/js-reanimated/index.js
index 65873f83e1ddf9b865cc1ae3145fb5d3de9e6ff0..6db0697d6704a53a76b1dadb25da3488651fa0ad 100644
--- a/lib/module/ReanimatedModule/js-reanimated/index.js
+++ b/lib/module/ReanimatedModule/js-reanimated/index.js
@@ -6,8 +6,11 @@ export { createJSReanimatedModule } from "./JSReanimated.js";
 // TODO: Move these functions outside of index file.
 export const _updatePropsJS = (updates, viewRef, isAnimatedProps) => {
   if (viewRef) {
-    const component = viewRef.getAnimatableRef ? viewRef.getAnimatableRef() : viewRef;
-    const [rawStyles] = Object.keys(updates).reduce(
+    const component = getComponentFromRef(viewRef);
+    if (!component) {
+      return;
+    }
+    const [rawStyles] = Object.keys(updates ?? {}).reduce(
     // eslint-disable-next-line @typescript-eslint/no-explicit-any
     (acc, key) => {
       const value = updates[key];
@@ -20,11 +23,11 @@ export const _updatePropsJS = (updates, viewRef, isAnimatedProps) => {
       // Also, some components (e.g. from react-native-svg) don't have styles
       // and always provide setNativeProps function instead (even on React Native Web 0.19+).
       setNativeProps(component, rawStyles, isAnimatedProps);
-    } else if (createReactDOMStyle !== undefined && component.style !== undefined) {
+    } else if (component.style !== undefined) {
       // React Native Web 0.19+ no longer provides setNativeProps function,
       // so we need to update DOM nodes directly.
       updatePropsDOM(component, rawStyles, isAnimatedProps);
-    } else if (Object.keys(component.props).length > 0) {
+    } else if (component.props && Object.keys(component.props).length > 0 && component._touchableNode) {
       Object.keys(component.props).forEach(key => {
         if (!rawStyles[key]) {
           return;
@@ -38,6 +41,16 @@ export const _updatePropsJS = (updates, viewRef, isAnimatedProps) => {
     }
   }
 };
+const getComponentFromRef = viewRef => {
+  const component = viewRef.getAnimatableRef ? viewRef.getAnimatableRef() : viewRef;
+  if (!component) {
+    return component;
+  }
+  if (component.host) {
+    return component.host;
+  }
+  return component.current?.host ?? component;
+};
 const setNativeProps = (component, newProps, isAnimatedProps) => {
   if (isAnimatedProps) {
     // Only update UI props directly on the component,
@@ -61,9 +74,9 @@ const updatePropsDOM = (component, style, isAnimatedProps) => {
     ...style
   };
   component.previousStyle = currentStyle;
-  const domStyle = createReactDOMStyle(currentStyle);
-  if (Array.isArray(domStyle.transform) && createTransformValue !== undefined) {
-    domStyle.transform = createTransformValue(domStyle.transform);
+  const domStyle = createReactDOMStyle ? createReactDOMStyle(currentStyle) : currentStyle;
+  if (Array.isArray(domStyle.transform)) {
+    domStyle.transform = createTransformValue ? createTransformValue(domStyle.transform) : createTransformValueFallback(domStyle.transform);
   }
   if (createTextShadowValue !== undefined && (domStyle.textShadowColor || domStyle.textShadowRadius || domStyle.textShadowOffset)) {
     domStyle.textShadow = createTextShadowValue({
@@ -87,4 +100,24 @@ const updatePropsDOM = (component, style, isAnimatedProps) => {
     }
   }
 };
+const TRANSFORM_UNITS = {
+  perspective: 'px',
+  rotate: 'deg',
+  rotateX: 'deg',
+  rotateY: 'deg',
+  rotateZ: 'deg',
+  skewX: 'deg',
+  skewY: 'deg',
+  translateX: 'px',
+  translateY: 'px',
+  translateZ: 'px'
+};
+const createTransformValueFallback = transform => transform.map(transformProp => {
+  const [key, value] = Object.entries(transformProp)[0] ?? [];
+  if (key == null) {
+    return '';
+  }
+  const unit = typeof value === 'number' ? TRANSFORM_UNITS[key] ?? '' : '';
+  return `${key}(${value}${unit})`;
+}).filter(Boolean).join(' ');
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/lib/module/createAnimatedComponent/InlinePropManager.js b/lib/module/createAnimatedComponent/InlinePropManager.js
index d20b8c95439ed04acba518b62132254b9075c025..e3ff29d59c43f7217585712463ce09ee4ca5df85 100644
--- a/lib/module/createAnimatedComponent/InlinePropManager.js
+++ b/lib/module/createAnimatedComponent/InlinePropManager.js
@@ -12,11 +12,13 @@ function isInlineStyleTransform(transform) {
   return transform.some(t => hasInlineStyles(t));
 }
 function inlinePropsHasChanged(styles1, styles2) {
-  if (Object.keys(styles1).length !== Object.keys(styles2).length) {
+  const s1 = styles1 ?? {};
+  const s2 = styles2 ?? {};
+  if (Object.keys(s1).length !== Object.keys(s2).length) {
     return true;
   }
-  for (const key of Object.keys(styles1)) {
-    if (styles1[key] !== styles2[key]) {
+  for (const key of Object.keys(s1)) {
+    if (s1[key] !== s2[key]) {
       return true;
     }
   }
@@ -81,6 +83,9 @@ export function getInlineStyle(style, isFirstRender) {
     return getInlinePropsUpdate(style);
   }
   const newStyle = {};
+  if (style == null) {
+    return newStyle;
+  }
   for (const [key, styleValue] of Object.entries(style)) {
     if (!isSharedValue(styleValue) && !(key === 'transform' && isInlineStyleTransform(styleValue))) {
       newStyle[key] = styleValue;
diff --git a/src/ReanimatedModule/js-reanimated/index.ts b/src/ReanimatedModule/js-reanimated/index.ts
index d90c2b3a5c9feb49f5c9c3036dc7ae9d2748c499..01b7480ca153de43eafb20f3e78ea6b2ae209ac1 100644
--- a/src/ReanimatedModule/js-reanimated/index.ts
+++ b/src/ReanimatedModule/js-reanimated/index.ts
@@ -14,8 +14,8 @@ interface JSReanimatedComponent {
   previousStyle: StyleProps;
   setNativeProps?: (style: StyleProps) => void;
   style?: StyleProps;
-  props: Record<string, string | number>;
-  _touchableNode: {
+  props?: Record<string, string | number>;
+  _touchableNode?: {
     setAttribute: (key: string, props: unknown) => void;
   };
 }
@@ -23,8 +23,8 @@ interface JSReanimatedComponent {
 export interface ReanimatedHTMLElement extends HTMLElement {
   previousStyle: StyleProps;
   setNativeProps?: (style: StyleProps) => void;
-  props: Record<string, string | number>;
-  _touchableNode: {
+  props?: Record<string, string | number>;
+  _touchableNode?: {
     setAttribute: (key: string, props: unknown) => void;
   };
   isDummy?: boolean;
@@ -41,10 +41,11 @@ export const _updatePropsJS = (
   isAnimatedProps?: boolean
 ): void => {
   if (viewRef) {
-    const component = viewRef.getAnimatableRef
-      ? viewRef.getAnimatableRef()
-      : viewRef;
-    const [rawStyles] = Object.keys(updates).reduce(
+    const component = getComponentFromRef(viewRef);
+    if (!component) {
+      return;
+    }
+    const [rawStyles] = Object.keys(updates ?? {}).reduce(
       // eslint-disable-next-line @typescript-eslint/no-explicit-any
       (acc: [StyleProps, AnimatedStyle<any>], key) => {
         const value = updates[key];
@@ -60,20 +61,21 @@ export const _updatePropsJS = (
       // Also, some components (e.g. from react-native-svg) don't have styles
       // and always provide setNativeProps function instead (even on React Native Web 0.19+).
       setNativeProps(component, rawStyles, isAnimatedProps);
-    } else if (
-      createReactDOMStyle !== undefined &&
-      component.style !== undefined
-    ) {
+    } else if (component.style !== undefined) {
       // React Native Web 0.19+ no longer provides setNativeProps function,
       // so we need to update DOM nodes directly.
       updatePropsDOM(component, rawStyles, isAnimatedProps);
-    } else if (Object.keys(component.props).length > 0) {
+    } else if (
+      component.props &&
+      Object.keys(component.props).length > 0 &&
+      component._touchableNode
+    ) {
       Object.keys(component.props).forEach((key) => {
         if (!rawStyles[key]) {
           return;
         }
         const dashedKey = key.replace(/[A-Z]/g, (m) => '-' + m.toLowerCase());
-        component._touchableNode.setAttribute(dashedKey, rawStyles[key]);
+        component._touchableNode!.setAttribute(dashedKey, rawStyles[key]);
       });
     } else {
       const componentName =
@@ -85,6 +87,29 @@ export const _updatePropsJS = (
   }
 };
 
+const getComponentFromRef = (
+  viewRef: (JSReanimatedComponent | ReanimatedHTMLElement) & {
+    getAnimatableRef?: () => JSReanimatedComponent | ReanimatedHTMLElement;
+    host?: JSReanimatedComponent | ReanimatedHTMLElement;
+    current?: {
+      host?: JSReanimatedComponent | ReanimatedHTMLElement;
+    };
+  }
+): JSReanimatedComponent | ReanimatedHTMLElement | undefined => {
+  const component = viewRef.getAnimatableRef
+    ? viewRef.getAnimatableRef()
+    : viewRef;
+  if (!component) {
+    return component;
+  }
+  if ('host' in component && component.host) {
+    return component.host;
+  }
+  return 'current' in component
+    ? component.current?.host ?? component
+    : component;
+};
+
 const setNativeProps = (
   component: JSReanimatedComponent | ReanimatedHTMLElement,
   newProps: StyleProps,
@@ -114,9 +139,13 @@ const updatePropsDOM = (
   const currentStyle = { ...previousStyle, ...style };
   (component as JSReanimatedComponent).previousStyle = currentStyle;
 
-  const domStyle = createReactDOMStyle(currentStyle);
-  if (Array.isArray(domStyle.transform) && createTransformValue !== undefined) {
-    domStyle.transform = createTransformValue(domStyle.transform);
+  const domStyle = createReactDOMStyle
+    ? createReactDOMStyle(currentStyle)
+    : currentStyle;
+  if (Array.isArray(domStyle.transform)) {
+    domStyle.transform = createTransformValue
+      ? createTransformValue(domStyle.transform)
+      : createTransformValueFallback(domStyle.transform);
   }
 
   if (
@@ -147,3 +176,31 @@ const updatePropsDOM = (
     }
   }
 };
+
+const TRANSFORM_UNITS: Partial<Record<string, string>> = {
+  perspective: 'px',
+  rotate: 'deg',
+  rotateX: 'deg',
+  rotateY: 'deg',
+  rotateZ: 'deg',
+  skewX: 'deg',
+  skewY: 'deg',
+  translateX: 'px',
+  translateY: 'px',
+  translateZ: 'px',
+};
+
+const createTransformValueFallback = (
+  transform: Array<Record<string, string | number>>
+): string =>
+  transform
+    .map((transformProp) => {
+      const [key, value] = Object.entries(transformProp)[0] ?? [];
+      if (key == null) {
+        return '';
+      }
+      const unit = typeof value === 'number' ? TRANSFORM_UNITS[key] ?? '' : '';
+      return `${key}(${value}${unit})`;
+    })
+    .filter(Boolean)
+    .join(' ');
diff --git a/src/createAnimatedComponent/InlinePropManager.ts b/src/createAnimatedComponent/InlinePropManager.ts
index 3f698bfccafee8289ce2c17d8e09dedeb0053fa0..6ab2b9a152bc1c12a9e69cd33913efe8db06f450 100644
--- a/src/createAnimatedComponent/InlinePropManager.ts
+++ b/src/createAnimatedComponent/InlinePropManager.ts
@@ -25,12 +25,14 @@ function inlinePropsHasChanged(
   styles1: StyleProps,
   styles2: StyleProps
 ): boolean {
-  if (Object.keys(styles1).length !== Object.keys(styles2).length) {
+  const s1 = styles1 ?? {};
+  const s2 = styles2 ?? {};
+  if (Object.keys(s1).length !== Object.keys(s2).length) {
     return true;
   }
 
-  for (const key of Object.keys(styles1)) {
-    if (styles1[key] !== styles2[key]) {
+  for (const key of Object.keys(s1)) {
+    if (s1[key] !== s2[key]) {
       return true;
     }
   }
@@ -114,6 +116,9 @@ export function getInlineStyle(
     return getInlinePropsUpdate(style) as Record<string, unknown>;
   }
   const newStyle: StyleProps = {};
+  if (style == null) {
+    return newStyle;
+  }
   for (const [key, styleValue] of Object.entries(style)) {
     if (
       !isSharedValue(styleValue) &&
