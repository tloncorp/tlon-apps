diff --git a/build/swift-ui/BottomSheet/index.d.ts b/build/swift-ui/BottomSheet/index.d.ts
index 52504c12fb31b4aa89da0914cd67405d1b34b867..4b29bdefb171e598a0d67f62029185b881d49517 100644
--- a/build/swift-ui/BottomSheet/index.d.ts
+++ b/build/swift-ui/BottomSheet/index.d.ts
@@ -1,3 +1,4 @@
+import { type PresentationDetent } from '../modifiers/presentationModifiers';
 import { type CommonViewModifierProps } from '../types';
 export interface BottomSheetProps extends CommonViewModifierProps {
     /**
@@ -25,6 +26,16 @@ export interface BottomSheetProps extends CommonViewModifierProps {
      * @default false
      */
     fitToContents?: boolean;
+    /** Available sheet heights managed directly by the native bottom sheet. */
+    detents?: PresentationDetent[];
+    /** The currently selected native sheet height. */
+    selectedDetent?: PresentationDetent;
+    /** Whether native selected-detent changes use the standard system animation. */
+    animateSelectedDetentChanges?: boolean;
+    /** Duration, in seconds, for native selected-detent height changes. */
+    detentAnimationDuration?: number;
+    /** Reserve the system close control's header area above generic content. */
+    reserveCloseButtonSpace?: boolean;
 }
 /**
  * `BottomSheet` presents content from the bottom of the screen.
diff --git a/build/swift-ui/modifiers/presentationModifiers.d.ts b/build/swift-ui/modifiers/presentationModifiers.d.ts
index 9936c0209b487ec6d59e4ffd7adcf8b4b96cd144..c007ec0bb4dc533797e253434189ed79cd0667e2 100644
--- a/build/swift-ui/modifiers/presentationModifiers.d.ts
+++ b/build/swift-ui/modifiers/presentationModifiers.d.ts
@@ -17,6 +17,7 @@ export type PresentationDetent = 'medium' | 'large' | {
  * @param options - Optional settings for tracking the selected detent.
  * @param options.selection - The currently selected detent.
  * @param options.onSelectionChange - Callback fired when the user changes the active detent by dragging.
+ * @param options.animationDuration - Duration, in seconds, for programmatic selection changes.
  * @platform ios 16.0+
  * @platform tvos 16.0+
  * @see Official [SwiftUI documentation](https://developer.apple.com/documentation/swiftui/view/presentationdetents(_:selection:)).
@@ -24,6 +25,7 @@ export type PresentationDetent = 'medium' | 'large' | {
 export declare const presentationDetents: (detents: PresentationDetent[], options?: {
     selection?: PresentationDetent;
     onSelectionChange?: (detent: PresentationDetent) => void;
+    animationDuration?: number;
 }) => ModifierConfig;
 /**
  * Controls the visibility of the drag indicator on a sheet.
diff --git a/ios/BottomSheetView.swift b/ios/BottomSheetView.swift
index 2aca8fb884fc63cb09e7ba9f5105ccdf9d8bcc41..05d707b927478d294eb6793ffe32f169ed5fc49c 100644
--- a/ios/BottomSheetView.swift
+++ b/ios/BottomSheetView.swift
@@ -6,6 +6,11 @@ import ExpoModulesCore
 final class BottomSheetProps: UIBaseViewProps {
   @Field var isPresented: Bool = false
   @Field var fitToContents: Bool = false
+  @Field var detents: [Either<PresentationDetentPreset, PresentationDetentItem>]?
+  @Field var selectedDetent: Either<PresentationDetentPreset, PresentationDetentItem>?
+  @Field var animateSelectedDetentChanges: Bool = false
+  @Field var detentAnimationDuration: Double = 0.4
+  @Field var reserveCloseButtonSpace: Bool = false
   var onIsPresentedChange = EventDispatcher()
   var onDismiss = EventDispatcher()
 }
@@ -58,6 +63,255 @@ private struct BottomSheetSizeReader<Content: View>: View {
   }
 }
 
+#if os(iOS)
+@available(iOS 16.0, *)
+private struct BottomSheetChromeContent<Content: View>: View {
+  @Environment(\.dismiss) private var dismiss
+  let content: Content
+  let reserveCloseButtonSpace: Bool
+
+  var body: some View {
+    ZStack(alignment: .topTrailing) {
+      // Keep generic React content below the system-style close control so
+      // full-width inputs and headers do not sit beneath its hit target.
+      content.padding(.top, reserveCloseButtonSpace ? 78 : 0)
+      SwiftUI.Button(action: { dismiss() }) {
+        SwiftUI.Image(systemName: "xmark.circle.fill")
+          .font(.system(size: 28))
+          .foregroundColor(Color(uiColor: .tertiaryLabel))
+      }
+      .buttonStyle(.plain)
+      .frame(width: 44, height: 44)
+      // Align the close control with the 40-point sheet header row, whose
+      // content begins 36 points below the sheet edge.
+      .padding(.top, 34)
+      .padding(.trailing, 8)
+      .accessibilityIdentifier("SheetCloseButton")
+    }
+    .presentationDragIndicator(.hidden)
+    .modifier(BottomSheetMaterialBackground())
+  }
+}
+
+@available(iOS 16.0, *)
+private struct BottomSheetMaterialBackground: ViewModifier {
+  @ViewBuilder
+  func body(content: Content) -> some View {
+    if #available(iOS 16.4, *) {
+      content.presentationBackground(.regularMaterial)
+    } else {
+      content
+    }
+  }
+}
+
+@available(iOS 16.0, *)
+private func bottomSheetHeightIdentifier(
+  _ height: CGFloat
+) -> UISheetPresentationController.Detent.Identifier {
+  UISheetPresentationController.Detent.Identifier("expo-ui-height-\(Int(height.rounded()))")
+}
+
+@available(iOS 16.0, *)
+private func bottomSheetHeight(
+  from identifier: UISheetPresentationController.Detent.Identifier?
+) -> CGFloat? {
+  let prefix = "expo-ui-height-"
+  guard
+    let rawValue = identifier?.rawValue,
+    rawValue.hasPrefix(prefix),
+    let height = Double(rawValue.dropFirst(prefix.count))
+  else {
+    return nil
+  }
+  return CGFloat(height)
+}
+
+@available(iOS 16.0, *)
+private func quantizedBottomSheetHeight(_ height: CGFloat) -> CGFloat {
+  let clampedHeight = min(max(height, 240), 800)
+  return (clampedHeight / 4).rounded() * 4
+}
+
+@available(iOS 16.0, *)
+private struct BottomSheetPresentationControllerConfigurator: UIViewControllerRepresentable {
+  let selectedHeight: Double?
+  let animateSelectionChanges: Bool
+  let animationDuration: Double
+
+  final class Coordinator: NSObject {
+    weak var presentationController: UISheetPresentationController?
+    var displayLink: CADisplayLink?
+    var currentHeight: CGFloat?
+    var targetHeight: CGFloat?
+    var startHeight: CGFloat = 0
+    var startTime: CFTimeInterval = 0
+    var duration: Double = 0
+
+    func select(
+      height: CGFloat,
+      in presentationController: UISheetPresentationController,
+      animated: Bool,
+      duration: Double
+    ) {
+      if
+        let targetHeight,
+        abs(targetHeight - height) < 1,
+        displayLink != nil
+      {
+        return
+      }
+
+      stopAnimation()
+      self.presentationController = presentationController
+      let resolvedCurrentHeight = currentHeight
+        ?? bottomSheetHeight(from: presentationController.selectedDetentIdentifier)
+        ?? height
+      currentHeight = resolvedCurrentHeight
+      targetHeight = height
+
+      guard animated, duration > 0, abs(resolvedCurrentHeight - height) > 1 else {
+        apply(height: height)
+        targetHeight = nil
+        return
+      }
+
+      startHeight = resolvedCurrentHeight
+      startTime = CACurrentMediaTime()
+      self.duration = duration
+      let displayLink = CADisplayLink(target: self, selector: #selector(step(_:)))
+      self.displayLink = displayLink
+      displayLink.add(to: .main, forMode: .common)
+    }
+
+    @objc private func step(_ displayLink: CADisplayLink) {
+      guard let targetHeight else {
+        stopAnimation()
+        return
+      }
+      let progress = min(max((displayLink.timestamp - startTime) / duration, 0), 1)
+      let easedProgress: Double
+      if progress < 0.5 {
+        easedProgress = 4 * progress * progress * progress
+      } else {
+        easedProgress = 1 - pow(-2 * progress + 2, 3) / 2
+      }
+      let height = startHeight + (targetHeight - startHeight) * CGFloat(easedProgress)
+      apply(height: height)
+
+      if progress >= 1 {
+        apply(height: targetHeight)
+        self.targetHeight = nil
+        stopAnimation()
+      }
+    }
+
+    private func apply(height: CGFloat) {
+      let height = quantizedBottomSheetHeight(height)
+      presentationController?.selectedDetentIdentifier = bottomSheetHeightIdentifier(height)
+      currentHeight = height
+    }
+
+    private func stopAnimation() {
+      displayLink?.invalidate()
+      displayLink = nil
+    }
+
+    deinit {
+      stopAnimation()
+    }
+  }
+
+  func makeCoordinator() -> Coordinator {
+    Coordinator()
+  }
+
+  func makeUIViewController(context: Context) -> UIViewController {
+    UIViewController()
+  }
+
+  func updateUIViewController(_ viewController: UIViewController, context: Context) {
+    let selectedHeight = selectedHeight.map { quantizedBottomSheetHeight(CGFloat($0)) }
+    let animateSelectionChanges = animateSelectionChanges
+    let animationDuration = max(animationDuration, 0)
+
+    DispatchQueue.main.async {
+      var ancestor: UIViewController? = viewController
+      var presentationController: UISheetPresentationController?
+      while let current = ancestor, presentationController == nil {
+        presentationController = current.sheetPresentationController
+        ancestor = current.parent
+      }
+      guard let presentationController, let selectedHeight else {
+        return
+      }
+
+      presentationController.prefersGrabberVisible = false
+
+      let firstHeight = CGFloat(240)
+      let firstIdentifier = bottomSheetHeightIdentifier(firstHeight)
+      let alreadyConfigured = presentationController.detents.contains {
+        $0.identifier == firstIdentifier
+      }
+      if !alreadyConfigured {
+        var detents = stride(from: 240, through: 800, by: 4).map { value in
+          let height = CGFloat(value)
+          return UISheetPresentationController.Detent.custom(
+            identifier: bottomSheetHeightIdentifier(height)
+          ) { _ in
+            height
+          }
+        }
+        detents.append(.large())
+        presentationController.detents = detents
+        context.coordinator.select(
+          height: selectedHeight,
+          in: presentationController,
+          animated: false,
+          duration: animationDuration
+        )
+        return
+      }
+
+      context.coordinator.select(
+        height: selectedHeight,
+        in: presentationController,
+        animated: animateSelectionChanges,
+        duration: animationDuration
+      )
+    }
+  }
+}
+
+@available(iOS 16.0, *)
+private struct BottomSheetDetentsContent<Content: View>: View {
+  @ObservedObject var props: BottomSheetProps
+  let content: Content
+
+  private var selectedHeight: Double? {
+    guard
+      let selectedDetent = props.selectedDetent,
+      let item: PresentationDetentItem = selectedDetent.get()
+    else {
+      return nil
+    }
+    return item.height
+  }
+
+  var body: some View {
+    content
+      .background {
+        BottomSheetPresentationControllerConfigurator(
+          selectedHeight: selectedHeight,
+          animateSelectionChanges: props.animateSelectedDetentChanges,
+          animationDuration: props.detentAnimationDuration
+        )
+        .frame(width: 0, height: 0)
+      }
+  }
+}
+#endif
+
 struct BottomSheetView: ExpoSwiftUI.View {
   @ObservedObject var props: BottomSheetProps
   @State private var isPresented: Bool = false
@@ -81,7 +335,15 @@ struct BottomSheetView: ExpoSwiftUI.View {
         content
       }
     } else {
+      #if os(iOS)
+      if #available(iOS 16.0, *), props.detents != nil {
+        BottomSheetDetentsContent(props: props, content: Children())
+      } else {
+        Children()
+      }
+      #else
       Children()
+      #endif
     }
   }
 
@@ -90,7 +352,18 @@ struct BottomSheetView: ExpoSwiftUI.View {
       .sheet(isPresented: $isPresented, onDismiss: {
         props.onDismiss()
       }) {
+        #if os(iOS)
+        if #available(iOS 16.0, *) {
+          BottomSheetChromeContent(
+            content: sheetContent,
+            reserveCloseButtonSpace: props.reserveCloseButtonSpace
+          )
+        } else {
+          sheetContent
+        }
+        #else
         sheetContent
+        #endif
       }
       .onChange(of: isPresented, perform: { newIsPresented in
         if props.isPresented == newIsPresented {
diff --git a/ios/Modifiers/PresentationModifiers.swift b/ios/Modifiers/PresentationModifiers.swift
index 8718921d38c951a0606a2954ea4c1cdc721cbe6a..245e6ccd97430f1c0e7eab17e86e6c12c51ba429 100644
--- a/ios/Modifiers/PresentationModifiers.swift
+++ b/ios/Modifiers/PresentationModifiers.swift
@@ -16,7 +16,7 @@ internal struct PresentationDetentItem: Record {
 }
 
 @available(iOS 16.0, tvOS 16.0, *)
-private func parsePresentationDetent(_ detent: Either<PresentationDetentPreset, PresentationDetentItem>) -> PresentationDetent? {
+internal func parsePresentationDetent(_ detent: Either<PresentationDetentPreset, PresentationDetentItem>) -> PresentationDetent? {
   if let preset: PresentationDetentPreset = detent.get() {
     switch preset {
     case .medium:
@@ -41,6 +41,7 @@ private func parsePresentationDetent(_ detent: Either<PresentationDetentPreset,
 internal struct PresentationDetentsModifier: ViewModifier, Record {
   @Field var detents: [Either<PresentationDetentPreset, PresentationDetentItem>]?
   @Field var selection: Either<PresentationDetentPreset, PresentationDetentItem>?
+  @Field var animationDuration: Double?
   var eventDispatcher: EventDispatcher?
 
   init() {}
@@ -71,6 +72,7 @@ internal struct PresentationDetentsModifier: ViewModifier, Record {
         detents: parseDetents(),
         rawDetents: detents ?? [],
         initialSelection: selection.flatMap { parsePresentationDetent($0) },
+        animationDuration: animationDuration,
         eventDispatcher: eventDispatcher
       ) {
         content
@@ -107,6 +109,7 @@ private struct PresentationDetentsSelectionView<WrappedContent: View>: View {
   let detents: Set<PresentationDetent>
   let detentSerializationMap: [PresentationDetent: Any]
   let initialSelection: PresentationDetent?
+  let animationDuration: Double?
   let eventDispatcher: EventDispatcher?
   let wrappedContent: WrappedContent
 
@@ -116,18 +119,39 @@ private struct PresentationDetentsSelectionView<WrappedContent: View>: View {
     detents: Set<PresentationDetent>,
     rawDetents: [Either<PresentationDetentPreset, PresentationDetentItem>],
     initialSelection: PresentationDetent?,
+    animationDuration: Double?,
     eventDispatcher: EventDispatcher?,
     @ViewBuilder content: () -> WrappedContent
   ) {
     self.detents = detents
     self.detentSerializationMap = buildDetentSerializationMap(from: rawDetents)
     self.initialSelection = initialSelection
+    self.animationDuration = animationDuration
     self.eventDispatcher = eventDispatcher
     self.wrappedContent = content()
     // Default to the first detent in the ordered array, not `detents.first` (the Set's order is
     // randomized per process), so the sheet always opens at the same detent.
     let firstOrderedDetent = rawDetents.compactMap { parsePresentationDetent($0) }.first
-    self._selectedDetent = State(initialValue: initialSelection ?? firstOrderedDetent ?? .large)
+    self._selectedDetent = State(initialValue: firstOrderedDetent ?? initialSelection ?? .large)
+  }
+
+  private func updateSelection(_ newSelection: PresentationDetent?) {
+    guard let newSelection, selectedDetent != newSelection else {
+      return
+    }
+
+    if let animationDuration, animationDuration > 0 {
+      // Expo can recreate the modifier-backed SwiftUI subtree when its props change. Defer the
+      // binding update until the replacement view is attached so UIKit receives an animated
+      // detent transition instead of opening the replacement directly at the target height.
+      DispatchQueue.main.async {
+        withAnimation(.easeInOut(duration: animationDuration)) {
+          selectedDetent = newSelection
+        }
+      }
+    } else {
+      selectedDetent = newSelection
+    }
   }
 
   var body: some View {
@@ -138,10 +162,11 @@ private struct PresentationDetentsSelectionView<WrappedContent: View>: View {
           eventDispatcher?(["presentationDetents": ["detent": serialized]])
         }
       }
+      .onAppear {
+        updateSelection(initialSelection)
+      }
       .onChange(of: initialSelection) { newSelection in
-        if let newSelection {
-          selectedDetent = newSelection
-        }
+        updateSelection(newSelection)
       }
   }
 }
diff --git a/src/community/bottom-sheet/BottomSheet.android.tsx b/src/community/bottom-sheet/BottomSheet.android.tsx
index f60ae61bf0c9023d41c55cdca2d9af0de7291a92..8788a8d22aa9db7e169fda65e49a5a790db04bc8 100644
--- a/src/community/bottom-sheet/BottomSheet.android.tsx
+++ b/src/community/bottom-sheet/BottomSheet.android.tsx
@@ -46,6 +46,17 @@ function findNearestSnapPointIndex(
   return typeof position === 'number' ? snapPoints.length - 1 : 0;
 }
 
+function getSingleSnapPointHeight(
+  snapPoints: BottomSheetProps['snapPoints'],
+  containerHeight: number
+): number | undefined {
+  if (!snapPoints || snapPoints.length !== 1) return undefined;
+
+  const parsed = parseSnapPoint(snapPoints[0]);
+  const height = parsed.type === 'fraction' ? parsed.value * containerHeight : parsed.value;
+  return Math.min(containerHeight, Math.max(1, height));
+}
+
 /**
  * Android implementation of `BottomSheet` using Material3 ModalBottomSheet.
  *
@@ -66,10 +77,13 @@ export function BottomSheet(props: BottomSheetProps) {
     backgroundStyle,
     children,
   } = props;
-  const { width } = useWindowDimensions();
+  const { width, height } = useWindowDimensions();
 
   const hasMultipleSnapPoints = snapPointsProp != null && snapPointsProp.length > 1;
   const fitToContents = enableDynamicSizing && (!snapPointsProp || snapPointsProp.length === 0);
+  const singleSnapPointHeight = enableDynamicSizing
+    ? undefined
+    : getSingleSnapPointHeight(snapPointsProp, height);
   const skipPartially = fitToContents || !hasMultipleSnapPoints;
   const maxIndex = snapPointsProp ? snapPointsProp.length - 1 : 0;
   const containerColor = extractBackgroundColor(backgroundStyle);
@@ -199,10 +213,17 @@ export function BottomSheet(props: BottomSheetProps) {
               shouldDismissOnBackPress: enablePanDownToClose,
               shouldDismissOnClickOutside: enablePanDownToClose,
             }}>
-            <RNHostView matchContents={fitToContents}>
+            <RNHostView matchContents={fitToContents || singleSnapPointHeight != null}>
               {/* flexGrow:1 + height:0 (flex-basis 0) fills RNHostView's measured height without
                   inheriting the scrollable child's content height, which would block scrolling to the end. */}
-              <View style={fitToContents ? undefined : { flexGrow: 1, height: 0 }}>
+              <View
+                style={
+                  singleSnapPointHeight != null
+                    ? { height: singleSnapPointHeight }
+                    : fitToContents
+                      ? undefined
+                      : { flexGrow: 1, height: 0 }
+                }>
                 <SheetScrollContextReset>{children}</SheetScrollContextReset>
               </View>
             </RNHostView>
diff --git a/src/community/bottom-sheet/BottomSheet.ios.tsx b/src/community/bottom-sheet/BottomSheet.ios.tsx
index 9908630d50d9a405688ae60c88f5e6638948420d..3808bdf2327cb2c240589caf0f64443ea848d344 100644
--- a/src/community/bottom-sheet/BottomSheet.ios.tsx
+++ b/src/community/bottom-sheet/BottomSheet.ios.tsx
@@ -235,7 +235,8 @@ export function BottomSheet(props: BottomSheetProps) {
           <NativeBottomSheet
             isPresented={isPresented}
             onIsPresentedChange={handlePresentedChange}
-            fitToContents={fitToContents}>
+            fitToContents={fitToContents}
+            reserveCloseButtonSpace>
             <Group modifiers={modifiers}>
               <RNHostView matchContents={fitToContents}>
                 {/* paddingTop compensates for tighter spacing between native drag indicator and content
diff --git a/src/swift-ui/BottomSheet/index.tsx b/src/swift-ui/BottomSheet/index.tsx
index cd8bad40beefa33e008f0bef895e894d48d72cdd..cdcbec460ee179cb409a0747e2b1d0a3c7e509ed 100644
--- a/src/swift-ui/BottomSheet/index.tsx
+++ b/src/swift-ui/BottomSheet/index.tsx
@@ -3,6 +3,7 @@ import { useState, type ComponentType } from 'react';
 import { type NativeSyntheticEvent } from 'react-native';
 
 import { createViewModifierEventListener } from '../modifiers/utils';
+import { type PresentationDetent } from '../modifiers/presentationModifiers';
 import { type CommonViewModifierProps } from '../types';
 
 export interface BottomSheetProps extends CommonViewModifierProps {
@@ -31,6 +32,16 @@ export interface BottomSheetProps extends CommonViewModifierProps {
    * @default false
    */
   fitToContents?: boolean;
+  /** Available sheet heights managed directly by the native bottom sheet. */
+  detents?: PresentationDetent[];
+  /** The currently selected native sheet height. */
+  selectedDetent?: PresentationDetent;
+  /** Whether native selected-detent changes use the standard system animation. */
+  animateSelectedDetentChanges?: boolean;
+  /** Duration, in seconds, for native selected-detent height changes. */
+  detentAnimationDuration?: number;
+  /** Reserve the system close control's header area above generic content. */
+  reserveCloseButtonSpace?: boolean;
 }
 
 type NativeBottomSheetProps = Omit<BottomSheetProps, 'onIsPresentedChange' | 'onDismiss'> & {
diff --git a/src/swift-ui/modifiers/presentationModifiers.ts b/src/swift-ui/modifiers/presentationModifiers.ts
index 66683ae042be9647673902347986c9fa027cb6fc..2fdfacf53ae08ee286ecd83003911cc651f6185c 100644
--- a/src/swift-ui/modifiers/presentationModifiers.ts
+++ b/src/swift-ui/modifiers/presentationModifiers.ts
@@ -19,6 +19,7 @@ export type PresentationDetent = 'medium' | 'large' | { fraction: number } | { h
  * @param options - Optional settings for tracking the selected detent.
  * @param options.selection - The currently selected detent.
  * @param options.onSelectionChange - Callback fired when the user changes the active detent by dragging.
+ * @param options.animationDuration - Duration, in seconds, for programmatic selection changes.
  * @platform ios 16.0+
  * @platform tvos 16.0+
  * @see Official [SwiftUI documentation](https://developer.apple.com/documentation/swiftui/view/presentationdetents(_:selection:)).
@@ -28,9 +29,14 @@ export const presentationDetents = (
   options?: {
     selection?: PresentationDetent;
     onSelectionChange?: (detent: PresentationDetent) => void;
+    animationDuration?: number;
   }
 ): ModifierConfig => {
-  const params = { detents, selection: options?.selection };
+  const params = {
+    detents,
+    selection: options?.selection,
+    animationDuration: options?.animationDuration,
+  };
 
   const { onSelectionChange } = options ?? {};
   if (onSelectionChange) {
