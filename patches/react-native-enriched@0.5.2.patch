diff --git a/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerDelegate.java b/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerDelegate.java
index d015c8f6686660cc295287f254f2323326028030..f083f2a2d92147c99dad09010c89b9b418511d35 100644
--- a/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerDelegate.java
+++ b/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerDelegate.java
@@ -94,6 +94,9 @@ public class EnrichedTextInputViewManagerDelegate<T extends View, U extends Base
       case "useHtmlNormalizer":
         mViewManager.setUseHtmlNormalizer(view, value == null ? false : (boolean) value);
         break;
+      case "textShortcuts":
+        mViewManager.setTextShortcuts(view, (ReadableArray) value);
+        break;
       default:
         super.setProperty(view, propName, value);
     }
diff --git a/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerInterface.java b/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerInterface.java
index ddc5617f00fa70a693893eba2d6d66ebf7a209bf..2452d1097622df91d9fd309613dc871b372cdc4d 100644
--- a/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerInterface.java
+++ b/android/generated/java/com/facebook/react/viewmanagers/EnrichedTextInputViewManagerInterface.java
@@ -39,6 +39,7 @@ public interface EnrichedTextInputViewManagerInterface<T extends View> extends V
   void setIsOnChangeTextSet(T view, boolean value);
   void setAndroidExperimentalSynchronousEvents(T view, boolean value);
   void setUseHtmlNormalizer(T view, boolean value);
+  void setTextShortcuts(T view, @Nullable ReadableArray value);
   void focus(T view);
   void blur(T view);
   void setValue(T view, String text);
diff --git a/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt b/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt
index 851e290cce3902e6a7acf28a7b763c2bed4644b0..bf6d4bbac444e2b076e8e3c703b109503ebc9c39 100644
--- a/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt
+++ b/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputView.kt
@@ -102,6 +102,8 @@ class EnrichedTextInputView : AppCompatEditText {
   var shouldEmitOnChangeText: Boolean = false
   var experimentalSynchronousEvents: Boolean = false
   var useHtmlNormalizer: Boolean = false
+  // Triple: (trigger, style, type) where type is "block" or "inline"
+  var textShortcuts: List<Triple<String, String, String>> = emptyList()
 
   var fontSize: Float? = null
   private var lineHeight: Float? = null
diff --git a/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputViewManager.kt b/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputViewManager.kt
index 1ba1279b1411c1a0df5079201f8cf0fc66371b4a..aca9408dfb5fdadbfce8aba4dbfc758474aeffaa 100644
--- a/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputViewManager.kt
+++ b/android/src/main/java/com/swmansion/enriched/textinput/EnrichedTextInputViewManager.kt
@@ -281,6 +281,23 @@ class EnrichedTextInputViewManager :
     view?.useHtmlNormalizer = value
   }
 
+  override fun setTextShortcuts(
+    view: EnrichedTextInputView?,
+    value: ReadableArray?,
+  ) {
+    val shortcuts = mutableListOf<Triple<String, String, String>>()
+    if (value != null) {
+      for (i in 0 until value.size()) {
+        val map = value.getMap(i) ?: continue
+        val trigger = map.getString("trigger") ?: continue
+        val style = map.getString("style") ?: continue
+        val type = map.getString("type") ?: "block"
+        shortcuts.add(Triple(trigger, style, type))
+      }
+    }
+    view?.textShortcuts = shortcuts
+  }
+
   override fun focus(view: EnrichedTextInputView?) {
     view?.requestFocusProgrammatically()
   }
diff --git a/android/src/main/java/com/swmansion/enriched/textinput/spans/EnrichedSpans.kt b/android/src/main/java/com/swmansion/enriched/textinput/spans/EnrichedSpans.kt
index f680cd5d96d1f73277c17c38567883c3fb44d15f..9e0f33fe96ca854dea4c0e5fb73815e262b01ebb 100644
--- a/android/src/main/java/com/swmansion/enriched/textinput/spans/EnrichedSpans.kt
+++ b/android/src/main/java/com/swmansion/enriched/textinput/spans/EnrichedSpans.kt
@@ -78,8 +78,8 @@ object EnrichedSpans {
 
   val listSpans: Map<String, ListSpanConfig> =
     mapOf(
-      UNORDERED_LIST to ListSpanConfig(EnrichedInputUnorderedListSpan::class.java, "- "),
-      ORDERED_LIST to ListSpanConfig(EnrichedInputOrderedListSpan::class.java, "1. "),
+      UNORDERED_LIST to ListSpanConfig(EnrichedInputUnorderedListSpan::class.java, null),
+      ORDERED_LIST to ListSpanConfig(EnrichedInputOrderedListSpan::class.java, null),
       CHECKBOX_LIST to ListSpanConfig(EnrichedInputCheckboxListSpan::class.java, null),
     )
 
diff --git a/android/src/main/java/com/swmansion/enriched/textinput/styles/ListStyles.kt b/android/src/main/java/com/swmansion/enriched/textinput/styles/ListStyles.kt
index 01801239e5b5d2a83ed0f346e3d85147a69348b3..c07f75dcf240a1e85359abae6932e2dc10d847d8 100644
--- a/android/src/main/java/com/swmansion/enriched/textinput/styles/ListStyles.kt
+++ b/android/src/main/java/com/swmansion/enriched/textinput/styles/ListStyles.kt
@@ -239,6 +239,138 @@ class ListStyles(
     }
   }
 
+  /**
+   * Maps a shortcut style name (from JS props) to its EnrichedSpans constant.
+   */
+  private fun resolveStyleName(name: String): String? = when (name) {
+    "h1" -> EnrichedSpans.H1
+    "h2" -> EnrichedSpans.H2
+    "h3" -> EnrichedSpans.H3
+    "h4" -> EnrichedSpans.H4
+    "h5" -> EnrichedSpans.H5
+    "h6" -> EnrichedSpans.H6
+    "blockquote" -> EnrichedSpans.BLOCK_QUOTE
+    "codeblock" -> EnrichedSpans.CODE_BLOCK
+    "unordered_list" -> EnrichedSpans.UNORDERED_LIST
+    "ordered_list" -> EnrichedSpans.ORDERED_LIST
+    "checkbox_list" -> EnrichedSpans.CHECKBOX_LIST
+    else -> null
+  }
+
+  /**
+   * Maps an inline shortcut style name to its EnrichedSpans constant.
+   */
+  private fun resolveInlineStyleName(name: String): String? = when (name) {
+    "bold" -> EnrichedSpans.BOLD
+    "italic" -> EnrichedSpans.ITALIC
+    "underline" -> EnrichedSpans.UNDERLINE
+    "strikethrough" -> EnrichedSpans.STRIKETHROUGH
+    "inline_code" -> EnrichedSpans.INLINE_CODE
+    else -> null
+  }
+
+  /**
+   * Handle configurable block text shortcuts. Checks if the text at the start of the
+   * current paragraph matches any configured trigger, and if so, removes the
+   * trigger text and applies the corresponding style.
+   */
+  private fun handleConfigurableShortcuts(
+    s: Editable,
+    endCursorPosition: Int,
+    previousTextLength: Int,
+  ) {
+    val shortcuts = view.textShortcuts
+    if (shortcuts.isEmpty()) return
+    if (previousTextLength >= s.length) return // only on insertion
+
+    val cursorPosition = endCursorPosition.coerceAtMost(s.length)
+    val (start, end) = s.getParagraphBounds(cursorPosition)
+    val paragraphText = s.substring(start, end)
+
+    for ((trigger, styleName, type) in shortcuts) {
+      if (type == "inline") continue // handled by handleInlineShortcuts
+      if (trigger.isEmpty()) continue
+      if (!paragraphText.startsWith(trigger)) continue
+
+      val resolvedStyle = resolveStyleName(styleName) ?: continue
+
+      // Replace the trigger text with a zero-width space and apply the style
+      s.replace(start, start + trigger.length, EnrichedConstants.ZWS_STRING)
+
+      // For list styles, use the list span system
+      val listConfig = EnrichedSpans.listSpans[resolvedStyle]
+      if (listConfig != null) {
+        setSpan(s, resolvedStyle, start, start + 1)
+        view.selection?.validateStyles()
+      } else {
+        // For paragraph styles (headings, blockquote, codeblock), use paragraphStyles
+        view.paragraphStyles?.toggleStyle(resolvedStyle)
+      }
+      return
+    }
+  }
+
+  /**
+   * Handle configurable inline text shortcuts. When a closing delimiter is typed,
+   * scans backwards for the opening delimiter. If found with text between,
+   * applies the inline style and removes both delimiters.
+   */
+  private fun handleInlineShortcuts(
+    s: Editable,
+    endCursorPosition: Int,
+    previousTextLength: Int,
+  ) {
+    val shortcuts = view.textShortcuts
+    if (shortcuts.isEmpty()) return
+    if (previousTextLength >= s.length) return // only on insertion
+
+    val cursorPosition = endCursorPosition.coerceAtMost(s.length)
+    val text = s.toString()
+    val (paraStart, _) = s.getParagraphBounds(cursorPosition)
+
+    for ((trigger, styleName, type) in shortcuts) {
+      if (type != "inline") continue
+      if (trigger.isEmpty()) continue
+
+      val resolvedStyle = resolveInlineStyleName(styleName) ?: continue
+
+      // Check if text ending at cursor matches the closing delimiter
+      if (cursorPosition < trigger.length) continue
+      val closingDelim = text.substring(cursorPosition - trigger.length, cursorPosition)
+      if (closingDelim != trigger) continue
+
+      val closeDelimStart = cursorPosition - trigger.length
+
+      // Scan backwards for opening delimiter (must be in same paragraph, with content between)
+      val searchEnd = closeDelimStart
+      val searchText = text.substring(paraStart, searchEnd)
+      val openIdx = searchText.lastIndexOf(trigger)
+      if (openIdx < 0) continue
+
+      val openAbsolute = paraStart + openIdx
+      val contentStart = openAbsolute + trigger.length
+      val contentEnd = closeDelimStart
+      if (contentEnd <= contentStart) continue
+
+      // Remove closing delimiter first (higher index)
+      s.delete(closeDelimStart, cursorPosition)
+      // Remove opening delimiter
+      s.delete(openAbsolute, openAbsolute + trigger.length)
+
+      // Calculate adjusted content range after deletions
+      val adjustedStart = openAbsolute
+      val adjustedEnd = contentEnd - trigger.length
+
+      // Apply inline style to the content
+      view.setCustomSelection(adjustedStart, adjustedEnd)
+      view.inlineStyles?.toggleStyle(resolvedStyle)
+
+      // Move cursor to end of styled text
+      view.setCustomSelection(adjustedEnd, adjustedEnd)
+      return
+    }
+  }
+
   fun afterTextChanged(
     s: Editable,
     endCursorPosition: Int,
@@ -247,6 +379,8 @@ class ListStyles(
     handleAfterTextChanged(s, EnrichedSpans.ORDERED_LIST, endCursorPosition, previousTextLength)
     handleAfterTextChanged(s, EnrichedSpans.UNORDERED_LIST, endCursorPosition, previousTextLength)
     handleAfterTextChanged(s, EnrichedSpans.CHECKBOX_LIST, endCursorPosition, previousTextLength)
+    handleConfigurableShortcuts(s, endCursorPosition, previousTextLength)
+    handleInlineShortcuts(s, endCursorPosition, previousTextLength)
   }
 
   fun getStyleRange(): Pair<Int, Int> = view.selection?.getParagraphSelection() ?: Pair(0, 0)
diff --git a/ios/EnrichedTextInputView.h b/ios/EnrichedTextInputView.h
index c2eb4a97e6c9470c68937ec03238ba6bc871f74b..83caed1ec3f7867434d7d194be58489911fe13dc 100644
--- a/ios/EnrichedTextInputView.h
+++ b/ios/EnrichedTextInputView.h
@@ -32,6 +32,8 @@ NS_ASSUME_NONNULL_BEGIN
   BOOL blockEmitting;
 @public
   BOOL useHtmlNormalizer;
+@public
+  NSArray<NSDictionary *> *textShortcuts;
 }
 - (CGSize)measureSize:(CGFloat)maxWidth;
 - (void)emitOnLinkDetectedEvent:(NSString *)text
diff --git a/ios/EnrichedTextInputView.mm b/ios/EnrichedTextInputView.mm
index 5e81c9f26df463cdada4da2bb51f83fa6225760e..5c8eb9731c190c6cdbe42c663987c50f2ca2580e 100644
--- a/ios/EnrichedTextInputView.mm
+++ b/ios/EnrichedTextInputView.mm
@@ -9,6 +9,7 @@
 #import "TextBlockTapGestureRecognizer.h"
 #import "UIView+React.h"
 #import "WordsUtils.h"
+#import "TextInsertionUtils.h"
 #import "ZeroWidthSpaceUtils.h"
 #import <React/RCTConversions.h>
 #import <ReactNativeEnriched/EnrichedTextInputViewComponentDescriptor.h>
@@ -761,6 +762,22 @@ - (void)updateProps:(Props::Shared const &)props
     useHtmlNormalizer = newViewProps.useHtmlNormalizer;
   }
 
+  // textShortcuts
+  if (newViewProps.textShortcuts != oldViewProps.textShortcuts) {
+    NSMutableArray *shortcuts = [NSMutableArray new];
+    for (const auto &item : newViewProps.textShortcuts) {
+      NSString *type = item.type.empty()
+          ? @"block"
+          : [NSString fromCppString:item.type];
+      [shortcuts addObject:@{
+        @"trigger" : [NSString fromCppString:item.trigger],
+        @"style" : [NSString fromCppString:item.style],
+        @"type" : type
+      }];
+    }
+    textShortcuts = shortcuts;
+  }
+
   // default value - must be set before placeholder to make sure it correctly
   // shows on first mount
   if (newViewProps.defaultValue != oldViewProps.defaultValue) {
@@ -1966,6 +1983,269 @@ - (void)handleKeyPressInRange:(NSString *)text range:(NSRange)range {
   }
 }
 
+/**
+ * Maps a shortcut style name (from JS props) to its native StyleType enum value.
+ */
++ (NSNumber *_Nullable)styleTypeForName:(NSString *)name {
+  static NSDictionary<NSString *, NSNumber *> *map = nil;
+  static dispatch_once_t onceToken;
+  dispatch_once(&onceToken, ^{
+    map = @{
+      @"h1" : @(H1),
+      @"h2" : @(H2),
+      @"h3" : @(H3),
+      @"h4" : @(H4),
+      @"h5" : @(H5),
+      @"h6" : @(H6),
+      @"blockquote" : @(BlockQuote),
+      @"codeblock" : @(CodeBlock),
+      @"unordered_list" : @(UnorderedList),
+      @"ordered_list" : @(OrderedList),
+      @"checkbox_list" : @(CheckboxList),
+    };
+  });
+  return map[name];
+}
+
+/**
+ * Generic handler for configurable text shortcuts.
+ * Checks if the text being inserted completes a configured shortcut pattern
+ * at the start of the current paragraph, following the same approach as the
+ * built-in list shortcuts (tryHandlingListShorcutInRange).
+ */
+- (BOOL)tryHandlingTextShortcutInRange:(NSRange)range
+                       replacementText:(NSString *)text {
+  if (textShortcuts == nil || textShortcuts.count == 0) {
+    return NO;
+  }
+
+  NSString *fullText = textView.textStorage.string;
+  NSRange paragraphRange = [fullText paragraphRangeForRange:range];
+
+  for (NSDictionary *shortcut in textShortcuts) {
+    NSString *shortcutType = shortcut[@"type"];
+    if ([shortcutType isEqualToString:@"inline"]) {
+      continue; // handled by tryHandlingInlineShortcutInRange
+    }
+
+    NSString *trigger = shortcut[@"trigger"];
+    NSString *styleName = shortcut[@"style"];
+    if (trigger == nil || styleName == nil || trigger.length == 0) {
+      continue;
+    }
+
+    // The trigger is what the paragraph should look like AFTER this insertion.
+    // E.g., for trigger "# ", the user has typed "#" and is now inserting " ".
+    // So the paragraph text before cursor is trigger minus the last char,
+    // and the inserted text is the last char of the trigger.
+    NSString *lastTriggerChar =
+        [trigger substringFromIndex:trigger.length - 1];
+    NSString *prefixBeforeCursor =
+        [trigger substringToIndex:trigger.length - 1];
+
+    // Check: the inserted text must match the last character of the trigger
+    if (![text isEqualToString:lastTriggerChar]) {
+      continue;
+    }
+
+    // Check: the text before cursor in this paragraph must match the prefix
+    NSInteger charsBeforeCursor = range.location - paragraphRange.location;
+    if (charsBeforeCursor != (NSInteger)prefixBeforeCursor.length) {
+      continue;
+    }
+
+    if (prefixBeforeCursor.length > 0) {
+      NSString *paragraphPrefix =
+          [fullText substringWithRange:NSMakeRange(paragraphRange.location,
+                                                   prefixBeforeCursor.length)];
+      if (![paragraphPrefix isEqualToString:prefixBeforeCursor]) {
+        continue;
+      }
+    }
+
+    // We have a match — resolve the style type
+    NSNumber *styleType =
+        [EnrichedTextInputView styleTypeForName:styleName];
+    if (styleType == nil) {
+      continue;
+    }
+
+    // Apply the style (handles conflicts with other block styles)
+    if ([self handleStyleBlocksAndConflicts:(StyleType)[styleType integerValue]
+                                      range:paragraphRange]) {
+      // Remove the trigger prefix text
+      blockEmitting = YES;
+      [TextInsertionUtils
+              replaceText:@""
+                       at:NSMakeRange(paragraphRange.location,
+                                      prefixBeforeCursor.length)
+       additionalAttributes:nullptr
+                    input:self
+            withSelection:YES];
+      blockEmitting = NO;
+
+      // Add style attributes to the paragraph (minus the removed prefix)
+      id<BaseStyleProtocol> style = stylesDict[styleType];
+      if (style != nil) {
+        NSRange newParagraphRange = NSMakeRange(
+            paragraphRange.location,
+            paragraphRange.length - prefixBeforeCursor.length);
+        [style addAttributes:newParagraphRange withTypingAttr:YES];
+      }
+      return YES;
+    }
+  }
+
+  return NO;
+}
+
+/**
+ * Maps an inline shortcut style name to its native StyleType enum value.
+ */
++ (NSNumber *_Nullable)inlineStyleTypeForName:(NSString *)name {
+  static NSDictionary<NSString *, NSNumber *> *map = nil;
+  static dispatch_once_t onceToken;
+  dispatch_once(&onceToken, ^{
+    map = @{
+      @"bold" : @(Bold),
+      @"italic" : @(Italic),
+      @"underline" : @(Underline),
+      @"strikethrough" : @(Strikethrough),
+      @"inline_code" : @(InlineCode),
+    };
+  });
+  return map[name];
+}
+
+/**
+ * Handler for inline text shortcuts (e.g. `code`, **bold**, *italic*).
+ * When the closing delimiter is typed, scans backwards for the opening
+ * delimiter. If found with text between, applies the inline style and
+ * removes both delimiters.
+ */
+- (BOOL)tryHandlingInlineShortcutInRange:(NSRange)range
+                         replacementText:(NSString *)text {
+  if (textShortcuts == nil || textShortcuts.count == 0) {
+    return NO;
+  }
+
+  NSString *fullText = textView.textStorage.string;
+
+  for (NSDictionary *shortcut in textShortcuts) {
+    NSString *shortcutType = shortcut[@"type"];
+    if (![shortcutType isEqualToString:@"inline"]) {
+      continue;
+    }
+
+    NSString *trigger = shortcut[@"trigger"];
+    NSString *styleName = shortcut[@"style"];
+    if (trigger == nil || styleName == nil || trigger.length == 0) {
+      continue;
+    }
+
+    // Check if the inserted text completes the closing delimiter
+    // For multi-char delimiters (like "**"), the last char is being inserted
+    // and the rest should be right before the cursor
+    NSString *lastTriggerChar =
+        [trigger substringFromIndex:trigger.length - 1];
+    if (![text isEqualToString:lastTriggerChar]) {
+      continue;
+    }
+
+    // Check if chars before cursor match the rest of the closing delimiter
+    NSInteger delimPrefixLen = trigger.length - 1;
+    if (delimPrefixLen > 0) {
+      if ((NSInteger)range.location < delimPrefixLen) {
+        continue;
+      }
+      NSString *beforeCursor =
+          [fullText substringWithRange:NSMakeRange(range.location - delimPrefixLen,
+                                                   delimPrefixLen)];
+      if (![beforeCursor isEqualToString:
+              [trigger substringToIndex:delimPrefixLen]]) {
+        continue;
+      }
+    }
+
+    // The closing delimiter position (start of closing delimiter in text)
+    NSInteger closeDelimStart = range.location - delimPrefixLen;
+
+    // Now scan backwards from closeDelimStart to find the opening delimiter
+    // Must have at least one character between the delimiters
+    NSInteger searchStart = closeDelimStart - 1;
+    if (searchStart < (NSInteger)trigger.length) {
+      continue;
+    }
+
+    // Find the opening delimiter by scanning backwards
+    NSRange searchRange = NSMakeRange(0, closeDelimStart);
+    NSRange openRange = [fullText rangeOfString:trigger
+                                        options:NSBackwardsSearch
+                                          range:searchRange];
+    if (openRange.location == NSNotFound) {
+      continue;
+    }
+
+    // Must have content between the delimiters
+    NSInteger contentStart = openRange.location + trigger.length;
+    NSInteger contentEnd = closeDelimStart;
+    if (contentEnd <= contentStart) {
+      continue;
+    }
+
+    // Must be on the same paragraph
+    NSRange paragraphRange = [fullText paragraphRangeForRange:range];
+    if (openRange.location < paragraphRange.location) {
+      continue;
+    }
+
+    // Resolve the inline style type
+    NSNumber *styleType =
+        [EnrichedTextInputView inlineStyleTypeForName:styleName];
+    if (styleType == nil) {
+      continue;
+    }
+
+    blockEmitting = YES;
+
+    // Remove the closing delimiter prefix (chars before cursor that are part of delimiter)
+    if (delimPrefixLen > 0) {
+      [TextInsertionUtils
+              replaceText:@""
+                       at:NSMakeRange(closeDelimStart, delimPrefixLen)
+       additionalAttributes:nullptr
+                    input:self
+            withSelection:NO];
+      // Adjust contentEnd after removal
+      contentEnd -= delimPrefixLen;
+    }
+
+    // Remove the opening delimiter
+    [TextInsertionUtils
+            replaceText:@""
+                     at:openRange
+     additionalAttributes:nullptr
+                  input:self
+          withSelection:NO];
+    // Adjust content positions after removal
+    contentStart -= trigger.length;
+    contentEnd -= trigger.length;
+
+    blockEmitting = NO;
+
+    // Select the content range and apply the inline style
+    textView.selectedRange = NSMakeRange(contentStart, contentEnd - contentStart);
+    [self toggleRegularStyle:(StyleType)[styleType integerValue]];
+
+    // Move cursor to end of styled text
+    textView.selectedRange = NSMakeRange(contentEnd, 0);
+
+    return YES;
+  }
+
+  return NO;
+}
+
 - (bool)textView:(UITextView *)textView
     shouldChangeTextInRange:(NSRange)range
             replacementText:(NSString *)text {
@@ -1986,14 +2266,19 @@ - (bool)textView:(UITextView *)textView
   H5Style *h5Style = stylesDict[@([H5Style getStyleType])];
   H6Style *h6Style = stylesDict[@([H6Style getStyleType])];
 
+  // Check configurable text shortcuts (block: "# " → h1, inline: `code` → inline_code)
+  if ([self tryHandlingTextShortcutInRange:range replacementText:text] ||
+      [self tryHandlingInlineShortcutInRange:range replacementText:text]) {
+    [self anyTextMayHaveBeenModified];
+    return NO;
+  }
+
   // some of the changes these checks do could interfere with later checks and
   // cause a crash so here I rely on short circuiting evaluation of the logical
   // expression either way it's not possible to have two of them come off at the
   // same time
   if ([uStyle handleBackspaceInRange:range replacementText:text] ||
-      [uStyle tryHandlingListShorcutInRange:range replacementText:text] ||
       [oStyle handleBackspaceInRange:range replacementText:text] ||
-      [oStyle tryHandlingListShorcutInRange:range replacementText:text] ||
       [cbLStyle handleBackspaceInRange:range replacementText:text] ||
       [cbLStyle handleNewlinesInRange:range replacementText:text] ||
       [bqStyle handleBackspaceInRange:range replacementText:text] ||
diff --git a/lib/module/EnrichedTextInput.js b/lib/module/EnrichedTextInput.js
index da75a301a91297902caf3bb2590cc17ce73b05e2..5ba54135f4ec909f5fe1233ef7bf0ee6b6418dbd 100644
--- a/lib/module/EnrichedTextInput.js
+++ b/lib/module/EnrichedTextInput.js
@@ -37,6 +37,7 @@ export const EnrichedTextInput = ({
   onChangeSelection,
   onKeyPress,
   contextMenuItems,
+  textShortcuts,
   androidExperimentalSynchronousEvents = false,
   useHtmlNormalizer = false,
   scrollEnabled = true,
@@ -284,6 +285,7 @@ export const EnrichedTextInput = ({
     androidExperimentalSynchronousEvents: androidExperimentalSynchronousEvents,
     useHtmlNormalizer: useHtmlNormalizer,
     scrollEnabled: scrollEnabled,
+    textShortcuts: textShortcuts ?? [],
     ...rest
   });
 };
diff --git a/lib/typescript/src/EnrichedTextInput.d.ts b/lib/typescript/src/EnrichedTextInput.d.ts
index d3b12ecfef2f746f906b83e464c6eb874a8585e6..f0381a58bd89751d2403fc87e83ffbef56077930 100644
--- a/lib/typescript/src/EnrichedTextInput.d.ts
+++ b/lib/typescript/src/EnrichedTextInput.d.ts
@@ -77,6 +77,15 @@ export interface EnrichedTextInputProps extends Omit<ViewProps, 'children'> {
     onKeyPress?: (e: NativeSyntheticEvent<OnKeyPressEvent>) => void;
     onPasteImages?: (e: NativeSyntheticEvent<OnPasteImagesEvent>) => void;
     contextMenuItems?: ContextMenuItem[];
+    /**
+     * Configure markdown-like text shortcuts that auto-convert typed prefixes
+     * into block styles.
+     */
+    textShortcuts?: Array<{
+        trigger: string;
+        style: string;
+        type?: 'block' | 'inline';
+    }>;
     /**
      * If true, Android will use experimental synchronous events.
      * This will prevent from input flickering when updating component size.
diff --git a/src/EnrichedTextInput.tsx b/src/EnrichedTextInput.tsx
index ed6063eacef9cd9e871a58579dd532b6725a082f..3a286cc9bb1fa455addeff3355be9f32380a115f 100644
--- a/src/EnrichedTextInput.tsx
+++ b/src/EnrichedTextInput.tsx
@@ -128,6 +128,21 @@ export interface EnrichedTextInputProps extends Omit<ViewProps, 'children'> {
   onKeyPress?: (e: NativeSyntheticEvent<OnKeyPressEvent>) => void;
   onPasteImages?: (e: NativeSyntheticEvent<OnPasteImagesEvent>) => void;
   contextMenuItems?: ContextMenuItem[];
+  /**
+   * Configure text shortcuts that auto-convert typed patterns into styles.
+   *
+   * Two types of shortcuts are supported:
+   *
+   * **Block shortcuts** (type: 'block', default):
+   * Trigger at the start of a paragraph. E.g. typing "# " converts the line to H1.
+   * - style: "h1"-"h6", "blockquote", "codeblock", "unordered_list", "ordered_list", "checkbox_list"
+   *
+   * **Inline shortcuts** (type: 'inline'):
+   * Trigger when a closing delimiter is typed around text. E.g. typing `code` applies inline code.
+   * The trigger is the delimiter string (e.g. "`", "**", "*", "~~").
+   * - style: "bold", "italic", "strikethrough", "inline_code"
+   */
+  textShortcuts?: Array<{ trigger: string; style: string; type?: 'block' | 'inline' }>;
   /**
    * If true, Android will use experimental synchronous events.
    * This will prevent from input flickering when updating component size.
@@ -186,6 +201,7 @@ export const EnrichedTextInput = ({
   onChangeSelection,
   onKeyPress,
   contextMenuItems,
+  textShortcuts,
   androidExperimentalSynchronousEvents = false,
   useHtmlNormalizer = false,
   scrollEnabled = true,
@@ -460,6 +476,7 @@ export const EnrichedTextInput = ({
       }
       useHtmlNormalizer={useHtmlNormalizer}
       scrollEnabled={scrollEnabled}
+      textShortcuts={textShortcuts ?? []}
       {...rest}
     />
   );
diff --git a/src/spec/EnrichedTextInputNativeComponent.ts b/src/spec/EnrichedTextInputNativeComponent.ts
index 8bf5186aa4414485703ca3b86eca53803584911d..4780083b85d3161743f8b667899aa821da32643b 100644
--- a/src/spec/EnrichedTextInputNativeComponent.ts
+++ b/src/spec/EnrichedTextInputNativeComponent.ts
@@ -385,6 +385,9 @@ export interface NativeProps extends ViewProps {
   fontWeight?: string;
   fontStyle?: string;
 
+  // Configurable text shortcuts that auto-convert typed prefixes into block styles
+  textShortcuts: ReadonlyArray<Readonly<{ trigger: string; style: string; type?: string }>>;
+
   // Used for onChangeHtml event performance optimization
   isOnChangeHtmlSet: boolean;
   // Used for onChangeText event performance optimization
