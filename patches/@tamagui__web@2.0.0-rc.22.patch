diff --git a/dist/cjs/createTamagui.cjs b/dist/cjs/createTamagui.cjs
index 12353019f3826faf58ddd2b850e897112fea490d..cf9a8f18a522b205d26b7dd68aca241daa3601c4 100644
--- a/dist/cjs/createTamagui.cjs
+++ b/dist/cjs/createTamagui.cjs
@@ -125,6 +125,11 @@ ${JSON.stringify(variable, null, 2)}`);
     position: defaultPositionSetting
   });
   const defaultFontToken = defaultFont ? `$${defaultFont}` : "",
+    unset = {
+      ...configIn.unset
+    };
+  !unset.fontFamily && defaultFont && (unset.fontFamily = defaultFontToken);
+  const
     inputAnimations = configIn.animations,
     isMultiDriver = inputAnimations && typeof inputAnimations == "object" && "default" in inputAnimations && typeof inputAnimations.default?.useAnimations == "function",
     resolvedAnimations = isMultiDriver ? inputAnimations.default : inputAnimations,
@@ -134,6 +139,7 @@ ${JSON.stringify(variable, null, 2)}`);
       fontLanguages: [],
       media: {},
       ...configIn,
+      unset,
       // normalized animations (resolved from multi-driver format if needed)
       animations: resolvedAnimations ?? import_defaultAnimationDriver.defaultAnimationDriver,
       animationDrivers: isMultiDriver ? inputAnimations : void 0,
diff --git a/dist/cjs/createTamagui.native.js b/dist/cjs/createTamagui.native.js
index 03e1a4fa98c105d3ddc38612d7460755f1b11457..d22522dfdf9cb0f8ab94c706ec52a6aac724db81 100644
--- a/dist/cjs/createTamagui.native.js
+++ b/dist/cjs/createTamagui.native.js
@@ -169,6 +169,9 @@ ${JSON.stringify(variable, null, 2)}`);
     defaultPositionSetting = ((_configIn_settings1 = configIn.settings) === null || _configIn_settings1 === void 0 ? void 0 : _configIn_settings1.defaultPosition) || "static",
     defaultProps = configIn.defaultProps || {},
     defaultFontToken = defaultFont ? `$${defaultFont}` : "",
+    unset = {
+      ...configIn.unset
+    },
     inputAnimations = configIn.animations,
     isMultiDriver = inputAnimations && (typeof inputAnimations > "u" ? "undefined" : _type_of(inputAnimations)) === "object" && "default" in inputAnimations && typeof ((_inputAnimations_default = inputAnimations.default) === null || _inputAnimations_default === void 0 ? void 0 : _inputAnimations_default.useAnimations) == "function",
     resolvedAnimations = isMultiDriver ? inputAnimations.default : inputAnimations,
@@ -179,6 +182,7 @@ ${JSON.stringify(variable, null, 2)}`);
       fontLanguages: [],
       media: {},
       ...configIn,
+      unset,
       // normalized animations (resolved from multi-driver format if needed)
       animations: resolvedAnimations ?? import_defaultAnimationDriver.defaultAnimationDriver,
       animationDrivers,
@@ -207,6 +211,7 @@ ${JSON.stringify(variable, null, 2)}`);
       specificTokens,
       defaultFontToken
     };
+  !unset.fontFamily && defaultFont && (unset.fontFamily = defaultFontToken);
   if ((0, import_config.setConfig)(config), (0, import_useMedia.configureMedia)(config), process.env.NODE_ENV === "development") {
     var _process_env_DEBUG;
     !((_process_env_DEBUG = process.env.DEBUG) === null || _process_env_DEBUG === void 0) && _process_env_DEBUG.startsWith("tamagui") && console.info("Tamagui config:", config), globalThis.Tamagui || (globalThis.Tamagui = import_Tamagui.Tamagui);
diff --git a/dist/cjs/helpers/propMapper.cjs b/dist/cjs/helpers/propMapper.cjs
index 729ad0b0a7904363378e2c733b24a0e35305b1bc..dc89fe5d9ecf67fd22a82697e71f598dd7c26d23 100644
--- a/dist/cjs/helpers/propMapper.cjs
+++ b/dist/cjs/helpers/propMapper.cjs
@@ -46,9 +46,14 @@ const propMapper = (key, value, styleState, disabled, map) => {
         styleProps,
         staticConfig
       } = styleState,
-      {
-        variants
-      } = staticConfig;
+      originalValue = value;
+    if (value === "unset") {
+      const unsetVal = conf.unset?.[key];
+      if (unsetVal != null) value = unsetVal;else return;
+    }
+    const {
+      variants
+    } = staticConfig;
     if (!styleProps.noExpand && variants && key in variants) {
       const variantValue = resolveVariants(key, value, styleProps, styleState, "");
       if (variantValue) {
@@ -57,7 +62,6 @@ const propMapper = (key, value, styleState, disabled, map) => {
       }
     }
     styleProps.disableExpandShorthands || key in conf.shorthands && (key = conf.shorthands[key]);
-    const originalValue = value;
     if (value != null) if (typeof value == "string") {
       if (value[0] === "$") value = (0, import_getTokenForKey.getTokenForKey)(key, value, styleProps, styleState);else {
         const resolved = (0, import_resolveCompoundTokens.resolveCompoundTokens)(key, value, styleProps, styleState);
diff --git a/dist/cjs/helpers/propMapper.native.js b/dist/cjs/helpers/propMapper.native.js
index c99a906f36ada63214df79fea790edfcec3a26e0..35e86e18f4a1cadd6f6b313407ff2e4e54af1bd8 100644
--- a/dist/cjs/helpers/propMapper.native.js
+++ b/dist/cjs/helpers/propMapper.native.js
@@ -53,7 +53,12 @@ var propMapper = function (key, value, styleState, disabled, map) {
           styleProps,
           staticConfig
         } = styleState,
-        {
+        originalValue = value;
+      if (value === "unset") {
+        var unsetVal = conf.unset?.[key];
+        if (unsetVal != null) value = unsetVal;else return;
+      }
+      var {
           variants
         } = staticConfig;
       if (!styleProps.noExpand && variants && key in variants) {
@@ -67,7 +72,6 @@ var propMapper = function (key, value, styleState, disabled, map) {
         }
       }
       styleProps.disableExpandShorthands || key in conf.shorthands && (key = conf.shorthands[key]);
-      var originalValue = value;
       if (value != null) if (typeof value == "string") {
         if (value[0] === "$") value = (0, import_getTokenForKey.getTokenForKey)(key, value, styleProps, styleState);else {
           var resolved = (0, import_resolveCompoundTokens.resolveCompoundTokens)(key, value, styleProps, styleState);
diff --git a/dist/esm/createTamagui.mjs b/dist/esm/createTamagui.mjs
index dfb1545953e1d4540ea1e0d7c4cc43d6127c7875..f886a286ed6e75ac23a8dff3f4fb8d991c65f6bd 100644
--- a/dist/esm/createTamagui.mjs
+++ b/dist/esm/createTamagui.mjs
@@ -100,6 +100,11 @@ ${JSON.stringify(variable, null, 2)}`);
     position: defaultPositionSetting
   });
   const defaultFontToken = defaultFont ? `$${defaultFont}` : "",
+    unset = {
+      ...configIn.unset
+    };
+  !unset.fontFamily && defaultFont && (unset.fontFamily = defaultFontToken);
+  const
     inputAnimations = configIn.animations,
     isMultiDriver = inputAnimations && typeof inputAnimations == "object" && "default" in inputAnimations && typeof inputAnimations.default?.useAnimations == "function",
     resolvedAnimations = isMultiDriver ? inputAnimations.default : inputAnimations,
@@ -109,6 +114,7 @@ ${JSON.stringify(variable, null, 2)}`);
       fontLanguages: [],
       media: {},
       ...configIn,
+      unset,
       // normalized animations (resolved from multi-driver format if needed)
       animations: resolvedAnimations ?? defaultAnimationDriver,
       animationDrivers: isMultiDriver ? inputAnimations : void 0,
diff --git a/dist/esm/createTamagui.native.js b/dist/esm/createTamagui.native.js
index d53691f0e18764b7a70a10ca4d88670434963c92..0474a65ea2067a427da766ee4089e71216b479e5 100644
--- a/dist/esm/createTamagui.native.js
+++ b/dist/esm/createTamagui.native.js
@@ -142,6 +142,9 @@ ${JSON.stringify(variable, null, 2)}`);
     defaultPositionSetting = ((_configIn_settings1 = configIn.settings) === null || _configIn_settings1 === void 0 ? void 0 : _configIn_settings1.defaultPosition) || "static",
     defaultProps = configIn.defaultProps || {},
     defaultFontToken = defaultFont ? `$${defaultFont}` : "",
+    unset = {
+      ...configIn.unset
+    },
     inputAnimations = configIn.animations,
     isMultiDriver = inputAnimations && (typeof inputAnimations > "u" ? "undefined" : _type_of(inputAnimations)) === "object" && "default" in inputAnimations && typeof ((_inputAnimations_default = inputAnimations.default) === null || _inputAnimations_default === void 0 ? void 0 : _inputAnimations_default.useAnimations) == "function",
     resolvedAnimations = isMultiDriver ? inputAnimations.default : inputAnimations,
@@ -152,6 +155,7 @@ ${JSON.stringify(variable, null, 2)}`);
       fontLanguages: [],
       media: {},
       ...configIn,
+      unset,
       // normalized animations (resolved from multi-driver format if needed)
       animations: resolvedAnimations ?? defaultAnimationDriver,
       animationDrivers,
@@ -180,6 +184,7 @@ ${JSON.stringify(variable, null, 2)}`);
       specificTokens,
       defaultFontToken
     };
+  !unset.fontFamily && defaultFont && (unset.fontFamily = defaultFontToken);
   if (setConfig(config), configureMedia(config), process.env.NODE_ENV === "development") {
     var _process_env_DEBUG;
     !((_process_env_DEBUG = process.env.DEBUG) === null || _process_env_DEBUG === void 0) && _process_env_DEBUG.startsWith("tamagui") && console.info("Tamagui config:", config), globalThis.Tamagui || (globalThis.Tamagui = Tamagui);
diff --git a/dist/esm/helpers/propMapper.mjs b/dist/esm/helpers/propMapper.mjs
index 9c5100864d1cbce551212fa5205a88460ea72448..b9decf424ca48abd45672437ce5fd362b127db91 100644
--- a/dist/esm/helpers/propMapper.mjs
+++ b/dist/esm/helpers/propMapper.mjs
@@ -19,9 +19,14 @@ const propMapper = (key, value, styleState, disabled, map) => {
         styleProps,
         staticConfig
       } = styleState,
-      {
-        variants
-      } = staticConfig;
+      originalValue = value;
+    if (value === "unset") {
+      const unsetVal = conf.unset?.[key];
+      if (unsetVal != null) value = unsetVal;else return;
+    }
+    const {
+      variants
+    } = staticConfig;
     if (!styleProps.noExpand && variants && key in variants) {
       const variantValue = resolveVariants(key, value, styleProps, styleState, "");
       if (variantValue) {
@@ -30,7 +35,6 @@ const propMapper = (key, value, styleState, disabled, map) => {
       }
     }
     styleProps.disableExpandShorthands || key in conf.shorthands && (key = conf.shorthands[key]);
-    const originalValue = value;
     if (value != null) if (typeof value == "string") {
       if (value[0] === "$") value = getTokenForKey(key, value, styleProps, styleState);else {
         const resolved = resolveCompoundTokens(key, value, styleProps, styleState);
diff --git a/dist/esm/helpers/propMapper.native.js b/dist/esm/helpers/propMapper.native.js
index cf05699ca51720bc69a93b493eefed577c88d626..492c2205d8808fa90041384ef08209e4c55a82c3 100644
--- a/dist/esm/helpers/propMapper.native.js
+++ b/dist/esm/helpers/propMapper.native.js
@@ -24,7 +24,12 @@ var propMapper = function (key, value, styleState, disabled, map) {
           styleProps,
           staticConfig
         } = styleState,
-        {
+        originalValue = value;
+      if (value === "unset") {
+        var unsetVal = conf.unset?.[key];
+        if (unsetVal != null) value = unsetVal;else return;
+      }
+      var {
           variants
         } = staticConfig;
       if (!styleProps.noExpand && variants && key in variants) {
@@ -38,7 +43,6 @@ var propMapper = function (key, value, styleState, disabled, map) {
         }
       }
       styleProps.disableExpandShorthands || key in conf.shorthands && (key = conf.shorthands[key]);
-      var originalValue = value;
       if (value != null) if (typeof value == "string") {
         if (value[0] === "$") value = getTokenForKey(key, value, styleProps, styleState);else {
           var resolved = resolveCompoundTokens(key, value, styleProps, styleState);
diff --git a/src/createTamagui.ts b/src/createTamagui.ts
index 8019d40a120300cb09ec38aaaab7b5ead4ac602c..276f90a16386dc597835589bca3a579867e58bbc 100644
--- a/src/createTamagui.ts
+++ b/src/createTamagui.ts
@@ -191,6 +191,11 @@ export function createTamagui<Conf extends CreateTamaguiProps>(
 
   // ensure prefixed with $
   const defaultFontToken = defaultFont ? `$${defaultFont}` : ''
+  const unset = { ...configIn.unset }
+
+  if (!unset.fontFamily && defaultFont) {
+    unset.fontFamily = defaultFontToken
+  }
 
   // Text inherits font from root via CSS, no need for default fontFamily
   // only explicit fontFamily prop should add font_* class
@@ -216,6 +221,7 @@ export function createTamagui<Conf extends CreateTamaguiProps>(
     fontLanguages: [],
     media: {},
     ...configIn,
+    unset,
     // normalized animations (resolved from multi-driver format if needed)
     animations: resolvedAnimations ?? defaultAnimationDriver,
     animationDrivers,
diff --git a/src/helpers/propMapper.ts b/src/helpers/propMapper.ts
index b2551b61286e4bd11b3136509503cf7a8e786183..5c0a1fdcf75a59abd6ef1b6cf4e237ee2e8992af 100644
--- a/src/helpers/propMapper.ts
+++ b/src/helpers/propMapper.ts
@@ -40,6 +40,17 @@ export const propMapper: PropMapper = (key, value, styleState, disabled, map) =>
   }
 
   const { conf, styleProps, staticConfig } = styleState
+  const originalValue = value
+
+  if (value === 'unset') {
+    const unsetVal = conf.unset?.[key]
+    if (unsetVal != null) {
+      value = unsetVal
+    } else {
+      return
+    }
+  }
+
   const { variants } = staticConfig
 
   if (!styleProps.noExpand) {
@@ -59,9 +70,6 @@ export const propMapper: PropMapper = (key, value, styleState, disabled, map) =>
     }
   }
 
-  // Capture original value before resolution (for context prop tracking)
-  const originalValue = value
-
   if (value != null) {
     if (typeof value === 'string') {
       if (value[0] === '$') {
diff --git a/src/types.tsx b/src/types.tsx
index e050179804c0ecf349f2cd05674ecff1ee9aa128..28d4cc177c92aa801a7582b6e2550771ff460099 100644
--- a/src/types.tsx
+++ b/src/types.tsx
@@ -1277,6 +1277,7 @@ export type AnimationsConfigObject = {
 }
 
 export type CreateTamaguiProps = {
+  unset?: BaseStyleProps
   reactNative?: any
   shorthands?: CreateShorthands
   media?: GenericTamaguiConfig['media']
diff --git a/types/types.d.ts b/types/types.d.ts
index 91db21c597204ed9cb599424a94c716fac130733..7fa04a0a3a30365f229b515ce7e2a09515cfb926 100644
--- a/types/types.d.ts
+++ b/types/types.d.ts
@@ -767,6 +767,7 @@ export type AnimationsConfigObject = {
     [key: string]: AnimationDriver<any>;
 };
 export type CreateTamaguiProps = {
+    unset?: BaseStyleProps;
     reactNative?: any;
     shorthands?: CreateShorthands;
     media?: GenericTamaguiConfig['media'];
