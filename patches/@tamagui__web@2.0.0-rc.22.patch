diff --git a/dist/cjs/createComponent.cjs b/dist/cjs/createComponent.cjs
index e2db29df2dd40760694230d4ca3aaf38c4ff5630..f11c2f6b6ba923ecabe4e46b8e3415d2c85183b5 100644
--- a/dist/cjs/createComponent.cjs
+++ b/dist/cjs/createComponent.cjs
@@ -639,18 +639,28 @@ If you meant to do this, you can disable this warning - either change untilMeasu
       }
       process.env.NODE_ENV === "development" && time && time`spaced-as-child`;
       let content;
+      let resolvedChildren = children;
+      if (staticConfig.isZStack) {
+        const childArray = import_react.default.Children.toArray(children);
+        resolvedChildren = childArray.map((child, i) =>
+          import_react.default.createElement("div", {
+            key: import_react.default.isValidElement(child) && child.key != null ? child.key : `${i}0t`,
+            style: { display: "flex", flexDirection: "column", position: "absolute", top: 0, right: 0, bottom: 0, left: 0, pointerEvents: "box-none" }
+          }, child)
+        );
+      }
       if (isPassthrough) content = import_react.default.createElement(BaseComponent, {
         style: {
           display: "contents"
         }
       }, propsIn.children);else {
-        import_setupHooks.hooks.useChildren && (content = import_setupHooks.hooks.useChildren(elementType, content || children, viewProps));
+        import_setupHooks.hooks.useChildren && (content = import_setupHooks.hooks.useChildren(elementType, content || resolvedChildren, viewProps));
         const isRenderPropString = typeof renderProp == "string";
         if (renderProp && !isRenderPropString) {
-          const out = getCustomRender(renderProp, content || children, viewProps, componentState);
+          const out = getCustomRender(renderProp, content || resolvedChildren, viewProps, componentState);
           out && (viewProps = out.viewProps, elementType = out.elementType);
         }
-        content || (isRenderPropString && viewProps.render, content = import_react.default.createElement(elementType, viewProps, content || children)), process.env.NODE_ENV === "development" && time && time`use-children`;
+        content || (isRenderPropString && viewProps.render, content = import_react.default.createElement(elementType, viewProps, content || resolvedChildren)), process.env.NODE_ENV === "development" && time && time`use-children`;
       }
       const ResetPresence = animationDriver?.ResetPresence,
         needsReset = !!(
diff --git a/dist/cjs/createComponent.js b/dist/cjs/createComponent.js
index 43d732b550cb277df76ce4866c492cd344bf49a3..31547f0029b45d3dc9f54cce78790664c73f1e30 100644
--- a/dist/cjs/createComponent.js
+++ b/dist/cjs/createComponent.js
@@ -582,6 +582,16 @@ If you meant to do this, you can disable this warning - either change untilMeasu
     }
     process.env.NODE_ENV === "development" && time && time`spaced-as-child`;
     let content;
+    let resolvedChildren = children;
+    if (staticConfig.isZStack) {
+      const childArray = import_react.default.Children.toArray(children);
+      resolvedChildren = childArray.map((child, i) =>
+        import_react.default.createElement("div", {
+          key: import_react.default.isValidElement(child) && child.key != null ? child.key : `${i}0t`,
+          style: { display: "flex", flexDirection: "column", position: "absolute", top: 0, right: 0, bottom: 0, left: 0, pointerEvents: "box-none" }
+        }, child)
+      );
+    }
     if (isPassthrough)
       content = import_react.default.createElement(
         BaseComponent,
@@ -593,18 +603,18 @@ If you meant to do this, you can disable this warning - either change untilMeasu
         propsIn.children
       );
     else {
-      import_setupHooks.hooks.useChildren && (content = import_setupHooks.hooks.useChildren(elementType, content || children, viewProps));
+      import_setupHooks.hooks.useChildren && (content = import_setupHooks.hooks.useChildren(elementType, content || resolvedChildren, viewProps));
       const isRenderPropString = typeof renderProp == "string";
       if (renderProp && !isRenderPropString) {
         const out = getCustomRender(
           renderProp,
-          content || children,
+          content || resolvedChildren,
           viewProps,
           componentState
         );
         out && (viewProps = out.viewProps, elementType = out.elementType);
       }
-      content || (isRenderPropString && viewProps.render, content = import_react.default.createElement(elementType, viewProps, content || children)), process.env.NODE_ENV === "development" && time && time`use-children`;
+      content || (isRenderPropString && viewProps.render, content = import_react.default.createElement(elementType, viewProps, content || resolvedChildren)), process.env.NODE_ENV === "development" && time && time`use-children`;
     }
     const ResetPresence = animationDriver?.ResetPresence, needsReset = !!// not when passing down to child
     (!asChild && // not when passThrough
diff --git a/dist/cjs/createComponent.native.js b/dist/cjs/createComponent.native.js
index 0675dbb716af9c3b6c99e623823ccd5111cde074..3051d879ec973b42c909907a7bfeffa399eb788c 100644
--- a/dist/cjs/createComponent.native.js
+++ b/dist/cjs/createComponent.native.js
@@ -678,18 +678,28 @@ If you meant to do this, you can disable this warning - either change untilMeasu
       });
       process.env.NODE_ENV === "development" && time && time`spaced-as-child`;
       var content;
+      var resolvedChildren = children;
+      if (staticConfig.isZStack) {
+        var childArray = import_react.default.Children.toArray(children);
+        resolvedChildren = childArray.map(function(child, i) {
+          return import_react.default.createElement(BaseView || "div", {
+            key: import_react.default.isValidElement(child) && child.key != null ? child.key : "" + i + "0t",
+            style: { display: "flex", flexDirection: "column", position: "absolute", top: 0, right: 0, bottom: 0, left: 0, pointerEvents: "box-none" }
+          }, child);
+        });
+      }
       if (isPassthrough) content = /* @__PURE__ */import_react.default.createElement(BaseComponent, {
         style: {
           display: "contents"
         }
       }, propsIn.children);else {
-        import_setupHooks.hooks.useChildren && (content = import_setupHooks.hooks.useChildren(elementType, content || children, viewProps));
+        import_setupHooks.hooks.useChildren && (content = import_setupHooks.hooks.useChildren(elementType, content || resolvedChildren, viewProps));
         var isRenderPropString = typeof renderProp == "string";
         if (renderProp && !isRenderPropString) {
-          var out = getCustomRender(renderProp, content || children, viewProps, componentState);
+          var out = getCustomRender(renderProp, content || resolvedChildren, viewProps, componentState);
           out && (viewProps = out.viewProps, elementType = out.elementType);
         }
-        content || (isRenderPropString && viewProps.render, content = /* @__PURE__ */import_react.default.createElement(elementType, viewProps, content || children)), process.env.NODE_ENV === "development" && time && time`use-children`;
+        content || (isRenderPropString && viewProps.render, content = /* @__PURE__ */import_react.default.createElement(elementType, viewProps, content || resolvedChildren)), process.env.NODE_ENV === "development" && time && time`use-children`;
       }
       content = (0, import_eventHandling.wrapWithGestureDetector)(content, pressGesture, stateRef, isHOC);
       var ResetPresence = animationDriver?.ResetPresence,
diff --git a/dist/esm/createComponent.mjs b/dist/esm/createComponent.mjs
index d6b357519b52f9ecb9051847bc31c6c624bc474b..a23b745db0e8e988b136f43beb7fea636b4dd1ce 100644
--- a/dist/esm/createComponent.mjs
+++ b/dist/esm/createComponent.mjs
@@ -602,18 +602,28 @@ If you meant to do this, you can disable this warning - either change untilMeasu
       }
       process.env.NODE_ENV === "development" && time && time`spaced-as-child`;
       let content;
+      let resolvedChildren = children;
+      if (staticConfig.isZStack) {
+        const childArray = React.Children.toArray(children);
+        resolvedChildren = childArray.map((child, i) =>
+          React.createElement("div", {
+            key: React.isValidElement(child) && child.key != null ? child.key : `${i}0t`,
+            style: { display: "flex", flexDirection: "column", position: "absolute", top: 0, right: 0, bottom: 0, left: 0, pointerEvents: "box-none" }
+          }, child)
+        );
+      }
       if (isPassthrough) content = React.createElement(BaseComponent, {
         style: {
           display: "contents"
         }
       }, propsIn.children);else {
-        hooks.useChildren && (content = hooks.useChildren(elementType, content || children, viewProps));
+        hooks.useChildren && (content = hooks.useChildren(elementType, content || resolvedChildren, viewProps));
         const isRenderPropString = typeof renderProp == "string";
         if (renderProp && !isRenderPropString) {
-          const out = getCustomRender(renderProp, content || children, viewProps, componentState);
+          const out = getCustomRender(renderProp, content || resolvedChildren, viewProps, componentState);
           out && (viewProps = out.viewProps, elementType = out.elementType);
         }
-        content || (isRenderPropString && viewProps.render, content = React.createElement(elementType, viewProps, content || children)), process.env.NODE_ENV === "development" && time && time`use-children`;
+        content || (isRenderPropString && viewProps.render, content = React.createElement(elementType, viewProps, content || resolvedChildren)), process.env.NODE_ENV === "development" && time && time`use-children`;
       }
       const ResetPresence = animationDriver?.ResetPresence,
         needsReset = !!(
diff --git a/dist/esm/createComponent.native.js b/dist/esm/createComponent.native.js
index 1d9076bece48a4e1d43a61581f412da461bc719c..67f4b2f7834e8db0db19099435fba74e3a5e5ce0 100644
--- a/dist/esm/createComponent.native.js
+++ b/dist/esm/createComponent.native.js
@@ -639,18 +639,28 @@ If you meant to do this, you can disable this warning - either change untilMeasu
       });
       process.env.NODE_ENV === "development" && time && time`spaced-as-child`;
       var content;
+      var resolvedChildren = children;
+      if (staticConfig.isZStack) {
+        var childArray = React.Children.toArray(children);
+        resolvedChildren = childArray.map(function(child, i) {
+          return React.createElement(BaseView || "div", {
+            key: React.isValidElement(child) && child.key != null ? child.key : "" + i + "0t",
+            style: { display: "flex", flexDirection: "column", position: "absolute", top: 0, right: 0, bottom: 0, left: 0, pointerEvents: "box-none" }
+          }, child);
+        });
+      }
       if (isPassthrough) content = /* @__PURE__ */React.createElement(BaseComponent, {
         style: {
           display: "contents"
         }
       }, propsIn.children);else {
-        hooks.useChildren && (content = hooks.useChildren(elementType, content || children, viewProps));
+        hooks.useChildren && (content = hooks.useChildren(elementType, content || resolvedChildren, viewProps));
         var isRenderPropString = typeof renderProp == "string";
         if (renderProp && !isRenderPropString) {
-          var out = getCustomRender(renderProp, content || children, viewProps, componentState);
+          var out = getCustomRender(renderProp, content || resolvedChildren, viewProps, componentState);
           out && (viewProps = out.viewProps, elementType = out.elementType);
         }
-        content || (isRenderPropString && viewProps.render, content = /* @__PURE__ */React.createElement(elementType, viewProps, content || children)), process.env.NODE_ENV === "development" && time && time`use-children`;
+        content || (isRenderPropString && viewProps.render, content = /* @__PURE__ */React.createElement(elementType, viewProps, content || resolvedChildren)), process.env.NODE_ENV === "development" && time && time`use-children`;
       }
       content = wrapWithGestureDetector(content, pressGesture, stateRef, isHOC);
       var ResetPresence = animationDriver?.ResetPresence,
diff --git a/src/createComponent.tsx b/src/createComponent.tsx
index a875efe0fd894ca9f3321769b1ee93d06e0fef7d..e97826156719b95d2610ce0881ba12fdba570b0d 100644
--- a/src/createComponent.tsx
+++ b/src/createComponent.tsx
@@ -1431,6 +1431,29 @@ export function createComponent<
 
     let content: ReactNode | undefined
 
+    // ZStack fix: wrap each child in an absolute-fill container so they stack
+    // on top of each other. This was handled by spacedChildren in v1 but the
+    // call was removed in v2.
+    let resolvedChildren = children
+    if (staticConfig.isZStack) {
+      const childArray = React.Children.toArray(children)
+      resolvedChildren = childArray.map((child, i) => (
+        React.createElement('div', {
+          key: (React.isValidElement(child) && child.key != null) ? child.key : `${i}0t`,
+          style: {
+            display: 'flex',
+            flexDirection: 'column' as const,
+            position: 'absolute' as const,
+            top: 0,
+            right: 0,
+            bottom: 0,
+            left: 0,
+            pointerEvents: 'box-none' as const,
+          },
+        }, child)
+      ))
+    }
+
     if (isPassthrough) {
       // avoid re-parenting but avoid layout changes
       content = React.createElement(
@@ -1446,7 +1469,7 @@ export function createComponent<
       // here elementType is either the custom animated driver view, or base view
       if (hooks.useChildren) {
         // ONLY native:
-        content = hooks.useChildren(elementType, content || children, viewProps)
+        content = hooks.useChildren(elementType, content || resolvedChildren, viewProps)
       }
 
       const isRenderPropString = typeof renderProp === 'string'
@@ -1456,7 +1479,7 @@ export function createComponent<
       if (renderProp && !isRenderPropString) {
         const out = getCustomRender(
           renderProp,
-          content || children,
+          content || resolvedChildren,
           viewProps,
           componentState
         )
@@ -1472,7 +1495,7 @@ export function createComponent<
           viewProps.render === renderProp
         }
 
-        content = React.createElement(elementType, viewProps, content || children)
+        content = React.createElement(elementType, viewProps, content || resolvedChildren)
       }
 
       if (process.env.NODE_ENV === 'development' && time) time`use-children`
